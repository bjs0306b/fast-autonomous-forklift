# 📋 협업 규칙 (지게차 프로젝트 · S15P11A304)

## 1. Jira 운영 원칙

**계층은 Epic → Story 2단계만 사용한다.** (Task/Sub-task 안 씀)

| 단위 | 의미 | 예시 |
|------|------|------|
| **Epic** | 큰 기능 묶음 / 도메인 | `상품 거래 관리`, `사용자 인증` |
| **Story** | 실제 작업 단위 (= 브랜치 1개) | `상품 등록 기능`, `로그인 API` |

- **모든 Story는 반드시 Epic에 소속**시킨다. (백로그에서 Epic 하위로 정리)
- Story 하나 = 개발자가 **1~3일 안에 끝낼 수 있는 크기**로 쪼갠다. 너무 크면 Story를 나눈다.

## 2. Story 작성 규칙

**제목:** `[동사] + [대상]` 형태로 간결하게

```
✅ 상품 등록 기능 구현
✅ 상품 목록 검색 API
❌ 상품  (너무 모호)
```

**설명(Description) 템플릿:**

```
## 목적
사용자로서 ~할 수 있다. (한 줄 사용자 스토리)

## 인수 조건 (Acceptance Criteria)
- [ ] 조건 1
- [ ] 조건 2

## 참고
- 관련 화면/API 명세 링크 등
```

## 3. Story 상태 흐름

```
해야 할 일(To Do)  →  진행 중(In Progress)  →  완료(Done)
```

- 작업 **시작할 때** 본인이 담당자 지정 + `진행 중`으로 이동
- 코드가 **master에 머지되면** → GitLab 연동으로 **자동 `완료`** 처리 (아래 5번)

## 4. GitLab 브랜치 & 커밋 규칙 ⭐

**브랜치 네이밍** — 반드시 Story 키를 포함

```
{타입}/{이슈키}-{짧은설명}

예) feature/S15P11A304-4-product-register
    fix/S15P11A304-9-login-bug
```

타입: `feature` / `fix` / `refactor` / `docs`

**커밋 메시지** — 앞에 이슈 키를 붙인다

```
S15P11A304-4 상품 등록 폼 UI 구현
S15P11A304-4 상품 등록 API 연동
```

→ 이렇게만 써도 해당 Jira Story에 **커밋 링크가 자동으로 연결**됨.

## 5. 이슈 자동 완료 (Closes 키워드) ✅ *동작 검증 완료 (2026-07-14)*

Story를 끝내는 MR에는 **닫기 키워드 + 이슈 키**를 사용한다:

```
Closes S15P11A304-4
```

- 키워드는 `Closes` / `Fixes` / `Resolves` 모두 가능 (대소문자 무관)
- 브랜치 이름에 이슈 키가 있으면 MR 설명에 `Closes ...` 가 **자동으로 채워진다.**

→ 이슈 키가 포함된 **커밋/MR이 master 브랜치에 머지되는 순간** Jira Story가
**자동으로 `완료`**로 전환된다. (전환 경로: 해야 할 일 → 진행 중 → 완료)

⚠️ **주의**
- 전환은 **master 머지 시점**에 일어난다. 이슈 키가 들어간 브랜치를 master로 머지하면 완료 처리됨.
- **1 Story = 1 브랜치 = 1 MR** 원칙을 지킬 것. 한 Story를 여러 MR로 쪼개 머지하면
  **첫 머지에서 이미 완료**돼버릴 수 있다.
- 아직 진행 중인 작업이면 그 브랜치/MR을 master로 머지하지 말 것.

## 6. Merge Request 규칙

- **MR 제목**에도 이슈 키 포함: `S15P11A304-4 상품 등록 기능`
- master로 직접 push 금지 → **반드시 MR + 팀원 1명 이상 리뷰 후 머지**
- MR 설명에 완료 조건 체크 및 `Closes S15P11A304-4` 명시

## 7. 담당 & 데일리

- Story는 **한 명이 담당**한다 (담당자 지정 필수)
- 매일 스크럼 때 각자 **진행 중 Story** 기준으로 공유

## 8. ⚙️ GitLab ↔ Jira 연동 설정 (관리자 · 트러블슈팅)

자동 완료가 동작하려면 GitLab 쪽 연동이 올바르게 설정돼 있어야 한다.
**위치:** GitLab 프로젝트 → `Settings → Integrations → Jira issues`

| 항목 | 값 |
|------|-----|
| **Web URL** | `https://ssafy.atlassian.net` |
| Authentication | Basic |
| Email | 연동용 Jira 계정 이메일 |
| API token | 그 계정의 Jira API 토큰 (id.atlassian.com → Security → API tokens 에서 발급) |
| Trigger | ☑ Commit  ☑ Merge request |
| Enable comments | ☑ |
| Transition Jira issues | **Use custom transitions** = `11,21,31` (11=해야 할 일, 21=진행 중, 31=완료) |

- ⚠️ **Web URL 함정:** 반드시 **사이트 루트**(`https://ssafy.atlassian.net`)여야 한다.
  `.../projects/S15P11A304` 처럼 프로젝트 경로를 넣으면 API 호출이 전부 실패해 **아무 동작도 안 한다.**
  (실제로 초기 세팅이 이렇게 잘못돼 한동안 자동 전환이 안 됐었음)
- ⚠️ 연동에 쓰는 Jira 계정은 이 프로젝트에서 **이슈 전환·댓글 권한**이 있어야 한다.
  전환/댓글은 그 계정 이름으로 기록된다.

**동작 확인:** 설정 화면 하단 **Test settings** → `Connection successful` 이 떠야 정상.

**증상별 대처 — 머지했는데 완료가 안 됨:**
1. GitLab Jira 통합에서 **Test settings** 실행 → 실패 메시지 확인
2. Web URL이 사이트 루트인지, API 토큰이 유효한지 확인
3. MR/커밋에 이슈 키가 실제로 들어갔는지, master로 머지됐는지 확인

---

## ✅ 한 장 요약 (치트시트)

```
1. Jira에 Epic 만들고 → 그 아래 Story 생성
2. Story 잡으면 → 담당자 지정 + '진행 중'
3. 브랜치:  feature/S15P11A304-4-xxx
4. 커밋:    S15P11A304-4 작업 내용
5. 완료 MR: Closes S15P11A304-4 완료 → master 머지 → 자동 Done
```
