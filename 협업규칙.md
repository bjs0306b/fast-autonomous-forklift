# 📋 협업 규칙 (지게차 프로젝트 · S15P11A304)

## 1. Jira 운영 원칙

**계층은 Epic → Story/Task 2단계만 사용한다.** (Sub-task 안 씀)

| 단위 | 의미 | 예시 |
|------|------|------|
| **Epic** | 큰 기능 묶음 / 도메인 | `상품 거래 관리`, `사용자 인증` |
| **Story** | 실제 작업 단위 (= 브랜치 1개) | `상품 등록 기능`, `로그인 API` |
| **Task** | 기능이 아닌 기술·지원 작업 | `개발환경 구축`, `기술 조사` |

- **모든 Story/Task는 반드시 Epic에 소속**시킨다. (백로그에서 Epic 하위로 정리)

## 2. Story 작성 규칙

**제목:** `[동사] + [대상]` 형태로 간결하게

```
✅ 상품 등록 기능 구현
✅ 상품 목록 검색 API
❌ 상품  (너무 모호)
```

**설명(Description) 템플릿:**

```
## 목적
사용자로서 ~할 수 있다. (한 줄 사용자 스토리)

## 인수 조건 (Acceptance Criteria)
- [ ] 조건 1
- [ ] 조건 2

## 참고
- 관련 화면/API 명세 링크 등
```

## 3. Story 상태 흐름

```
해야 할 일(To Do)  →  진행 중(In Progress)  →  완료(Done)
```

- 작업 **시작할 때** 본인이 담당자 지정 + `진행 중`으로 이동
- 코드가 **develop(기본 브랜치)에 머지되면** → GitLab 연동으로 **자동 `완료`** 처리 (아래 5번)
  - master까지 갈 필요 없다. feature MR을 develop에 머지하는 순간 전환된다.

## 4. GitLab 브랜치 & 커밋 규칙 ⭐ (git-flow)

**브랜치 모델** — 2026-07-20 도입 (컨설턴트 가이드: git-flow 교과서적 활용)

```
master   ← 발표/배포용. 스프린트 종료 시에만 갱신 (release 머지)
develop  ← 통합 브랜치 = GitLab 기본 브랜치. 모든 feature가 여기로 머지
feature/{이슈키}-{설명}   ← 작업 단위. develop에서 분기 → develop으로 MR
release/sprint-{N}        ← 스프린트 종료 시 develop에서 분기 → master로 MR + 태그
hotfix/{이슈키}-{설명}    ← master 긴급 수정. master에서 분기 → master·develop 양쪽 머지
```

- **feature는 반드시 develop에서 분기**하고, MR 대상도 develop이다. (master 대상 MR은 release/hotfix만)
- 스프린트 종료 절차: `release/sprint-{N}` 생성 → master로 MR·머지 → 태그 `sprint-{N}` → develop에 역머지(필요 시).
- docs 브랜치도 develop 기준으로 작업한다.

**브랜치 네이밍** — 반드시 Story 키를 포함

```
{타입}/{이슈키}-{짧은설명}

예) feature/S15P11A304-4-product-register
    fix/S15P11A304-9-login-bug
```

타입: `feature` / `fix` / `refactor` / `docs` (+ git-flow의 `release` / `hotfix`)

- ⚠️ **Jira의 "브랜치 만들기" 버튼** 사용 시 이름이 `S15P11A304-N-...`으로 제안됨 — 생성 폼에서 앞에 **`feature/`를 직접 붙일 것** (분기 소스·MR 대상은 develop으로 자동 설정되므로 이름만 고치면 됨)

**커밋 메시지** — 앞에 이슈 키를 붙인다

```
S15P11A304-4 상품 등록 폼 UI 구현
S15P11A304-4 상품 등록 API 연동
```

→ 이렇게만 써도 해당 Jira Story에 **커밋 링크가 자동으로 연결**됨.

## 5. 이슈 자동 완료 (Closes 키워드) ✅ *develop 기준 재검증 완료 (2026-07-20)*

> 2026-07-14에 master 기준으로 최초 검증했고, git-flow 도입 후 2026-07-20에
> **develop 머지로 재검증**했다. MR !21(S15P11A304-65)을 develop에 머지한 지
> 4초 만에 Jira가 `진행 중` → `완료`로 전환됐다.

Story를 끝내는 MR에는 **닫기 키워드 + 이슈 키**를 사용한다:

```
Closes S15P11A304-4
```

- 키워드는 `Closes` / `Fixes` / `Resolves` 모두 가능 (대소문자 무관)
- 브랜치 이름에 이슈 키가 있으면 MR 설명에 `Closes ...` 가 **자동으로 채워진다.**

→ 이슈 키가 포함된 **커밋/MR이 기본 브랜치(develop)에 머지되는 순간** Jira Story가
**자동으로 `완료`**로 전환된다. (전환 경로: 해야 할 일 → 진행 중 → 완료)

⚠️ **주의**
- 닫기 키워드는 **GitLab 기본 브랜치 머지**에서 동작한다. git-flow 도입으로 기본 브랜치를
  **develop으로 변경**해야 feature 머지 = 즉시 완료가 유지된다. (기본 브랜치가 master인 채로 두면
  이슈가 스프린트 말 release 머지 때 한꺼번에 완료돼 번다운이 절벽이 된다.)
  **설정 위치:** GitLab → `Settings → Repository → Default branch` = `develop`
- **1 Story = 1 브랜치 = 1 MR** 원칙을 지킬 것. 한 Story를 여러 MR로 쪼개 머지하면
  **첫 머지에서 이미 완료**돼버릴 수 있다.
- 아직 진행 중인 작업이면 그 브랜치/MR을 develop으로 머지하지 말 것.
- release → master 머지 MR에는 닫기 키워드를 **쓰지 않는다** (이미 완료된 이슈 재전환 방지).

## 6. Merge Request 규칙

- **MR 제목**에도 이슈 키 포함: `S15P11A304-4 상품 등록 기능`
- master·develop으로 직접 push 금지 → **반드시 MR로 머지** (리뷰는 선택)
- feature MR 대상은 **develop**, release/hotfix MR만 master 대상
- MR 설명에 완료 조건 체크 및 `Closes S15P11A304-4` 명시

## 7. 담당 & 데일리

- Story는 **한 명이 담당**한다 (담당자 지정 필수)
- 매일 스크럼 때 각자 **진행 중 Story** 기준으로 공유

## 8. ⚙️ GitLab ↔ Jira 연동 설정 (관리자 · 트러블슈팅)

자동 완료가 동작하려면 GitLab 쪽 연동이 올바르게 설정돼 있어야 한다.
**위치:** GitLab 프로젝트 → `Settings → Integrations → Jira issues`

| 항목 | 값 |
|------|-----|
| **Web URL** | `https://ssafy.atlassian.net` |
| Authentication | Basic |
| Email | 연동용 Jira 계정 이메일 |
| API token | 그 계정의 Jira API 토큰 (id.atlassian.com → Security → API tokens 에서 발급) |
| Trigger | ☑ Commit  ☑ Merge request |
| Enable comments | ☑ |
| Transition Jira issues | **Use custom transitions** = `11,21,31` (11=해야 할 일, 21=진행 중, 31=완료) |

- ⚠️ **Web URL 함정:** 반드시 **사이트 루트**(`https://ssafy.atlassian.net`)여야 한다.
  `.../projects/S15P11A304` 처럼 프로젝트 경로를 넣으면 API 호출이 전부 실패해 **아무 동작도 안 한다.**
  (실제로 초기 세팅이 이렇게 잘못돼 한동안 자동 전환이 안 됐었음)
- ⚠️ 연동에 쓰는 Jira 계정은 이 프로젝트에서 **이슈 전환·댓글 권한**이 있어야 한다.
  전환/댓글은 그 계정 이름으로 기록된다.

**동작 확인:** 설정 화면 하단 **Test settings** → `Connection successful` 이 떠야 정상.

**증상별 대처 — 머지했는데 완료가 안 됨:**
1. **MR이 실제로 머지됐는지 먼저 확인** (`state: merged`). MR을 *생성*만 해도 Jira에
   개발 정보(브랜치·MR 링크)가 붙으면서 이슈의 `updated` 시각이 갱신되므로,
   뭔가 반영된 것처럼 보이지만 상태는 그대로다.
2. **기본 브랜치(develop)로 머지됐는지** 확인. 닫기 키워드는 기본 브랜치 머지에서만
   동작한다. 현재 기본 브랜치는 `develop`이다 (`Settings → Repository → Default branch`).
3. MR/커밋에 이슈 키와 닫기 키워드가 실제로 들어갔는지 확인
4. GitLab Jira 통합에서 **Test settings** 실행 → 실패 메시지 확인
5. Web URL이 사이트 루트인지, API 토큰이 유효한지 확인

> 로컬 `git branch -a`에 `origin/HEAD -> origin/master`로 보이더라도 그건 **로컬 캐시**다.
> 원격 기본 브랜치와 다를 수 있으니 `git remote set-head origin -a`로 갱신한다.

---

## ✅ 한 장 요약 (치트시트)

```
1. Jira에 Epic 만들고 → 그 아래 Story 생성
2. Story 잡으면 → 담당자 지정 + '진행 중'
3. 브랜치:  develop에서 분기 → feature/S15P11A304-4-xxx
4. 커밋:    S15P11A304-4 작업 내용
5. 완료 MR: Closes S15P11A304-4 → develop 머지 → 자동 Done
6. 스프린트 종료: release/sprint-N → master 머지 + 태그
```
