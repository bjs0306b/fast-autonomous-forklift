# 웹 관제 화면 와이어프레임 — FR-402 / FR-503 / 적재 화물 안전

> 대상: `frontend/monitoring-control-system` (Next.js 16 App Router, Tailwind v4)
> 기준일: 2026-07-29 · 실제 구현 코드 기준으로 작성(추정 아님)
> 이 문서는 **설계 산출물**이다. 코드 수정·Git 명령·서버 실행 없이 작성했다.

---

## 1. 현재 Next.js 화면 구조 분석

### 1.1 컴포넌트 트리 (실제 파일 기준)

```
app/page.tsx  (MonitoringPage — 유일한 라우트, "use client")
│
├── header                                   화면 제목 + dev 전용 영상상태 셀렉트
├── DashboardErrorBanner                     loadState === "error" 일 때만
├── CommandNotice                            안전 명령 결과 알림 (success/warning/error)
├── GlobalEmergencyStopBar                   전체 비상정지 + 대상 차량 수
│
└── grid  lg:[2fr_1fr]
    ├── MainRealtimeMonitoringView           ← 좌측 대형
    │   ├── DigitalTwinVideoLayer            영상 placeholder (실스트림 미연결)
    │   ├── (좌상단) 화면 정체성 라벨
    │   ├── (우상단) RealtimeConnectionBadge  STOMP 연결 상태
    │   │           ConnectionStatusBadge    영상 스트림 상태
    │   │           FullscreenButton
    │   │           SelectedVehicleOverlay   선택 차량 요약
    │   ├── overlay 슬롯 → LoadSafetyOverlay ★ WARNING/DANGER 시에만
    │   ├── (조건) 연결 중 오버레이  z-30
    │   └── (조건) 연결 실패 오버레이 z-30
    │
    └── grid-rows-2                          ← 우측
        ├── VehicleDetailPanel               우측 상단
        │   ├── 상태 배지 / 위치 dl
        │   ├── LoadSafetyPanel          ★ 적재 안전
        │   ├── 현재 작업
        │   └── STOP · EMERGENCY STOP 버튼
        └── MiniMap                          우측 하단
            └── MiniMapVehicleMarker[]
```

★ = 적재 화물 안전 기능으로 추가된 컴포넌트

### 1.2 상태·데이터 흐름

| 훅 | 책임 | 산출 |
|---|---|---|
| `useMonitoringDashboard` | REST 초기 조회 + 이벤트 반영 | `vehicles`, `selectedVehicle`, `selectedLoadSafety`, `loadState` |
| `useMonitoringSocket` | STOMP 구독 3개 | `connected` / `connecting` / `errored` |

**구독 토픽 (공통 토픽만 — 차량별 토픽은 중복이라 구독 안 함)**

```
/topic/vehicles/status        → applyStatusEvent
/topic/vehicles/location      → applyLocationEvent
/topic/vehicles/load-safety   → applyLoadSafetyEvent   ★
```

**REST**

```
GET /api/monitoring/dashboard              차량·작업 초기 상태
GET /api/vehicles/load-safety/latest       적재 안전 초기 상태 ★
POST /api/vehicles/{id}/commands/emergency-stop
POST /api/vehicles/commands/emergency-stop-all
```

### 1.3 이미 구현된 것 / 아직 없는 것

| 구분 | 항목 |
|---|---|
| **구현됨** | 차량 상태·위치 실시간, 미니맵 마커 이동, 선택 차량 상세, 개별·전체 비상정지(confirm·pending·부분실패 구분), 적재 안전 패널·오버레이, 연결 상태 배지 2종, 재연결 시 재조회 |
| **미구현** | **STOP 버튼 API 미연결**(`console.log`만), **RESET_ESTOP 전무**, 영상 실스트림(placeholder), 적재 안전 송신 측(수신 준비만 됨) |

### 1.4 확정 구조와의 일치 여부

| prompt64 확정 항목 | 현재 코드 | 일치 |
|---|---|---|
| 좌측 대형 = 카메라/디지털 트윈 | `MainRealtimeMonitoringView` (2fr) | ✅ |
| 우측 상단 = 선택 차량 상세 | `VehicleDetailPanel` | ✅ |
| 우측 하단 = 2D 미니맵 | `MiniMap` | ✅ |
| 하단 = 전체 비상정지 | `GlobalEmergencyStopBar` | ⚠ **현재는 상단 배치** |
| 사이드바 없음 | 없음 | ✅ |
| 2D/3D 토글 없음 | 없음 | ✅ |
| 미니맵 팝업 없음 | 마커 클릭 = 선택만 | ✅ |
| 배터리 없음 | 표시 안 함 | ✅ |

> ⚠ **차이 1건**: 전체 비상정지 바가 코드에서는 관제 본문 **위**에 있다. prompt64는 "하단"으로
> 명시했다. 본 와이어프레임은 **prompt64 확정안(하단)** 을 따르며, 이 차이를 §12 우선순위 P1로 둔다.

---

## 2. 사용자 흐름

### 2.1 기본 관제 루프

```
진입 → dashboard + load-safety REST 조회 → STOMP 연결
  → 미니맵에서 차량 선택
      → 우측 상단: 상태·위치·적재 안전
      → 좌측: 선택 차량 요약 오버레이
  → 실시간 이벤트 수신 시 해당 영역만 갱신
```

### 2.2 적재 위험 인지 흐름 (핵심)

```
비전/센서 → MQTT → 백엔드 → /topic/vehicles/load-safety
  → riskLevel 판정값 그대로 표시           ※ 프론트는 계산하지 않음
      ├ NORMAL / CAUTION → 패널에만 표시
      └ WARNING / DANGER → 패널 + 영상 위 경고 오버레이
          → 운영자가 원인(riskCode·메시지·기울기 수치) 확인
          → 운영자 판단으로 EMERGENCY STOP 클릭      ※ 자동 호출 없음
```

### 2.3 비상정지 흐름

```
[개별]  버튼 → confirm → POST → 응답 data.status 판정
          ├ PUBLISHED       success 알림 "명령 전송 완료"
          ├ PUBLISH_FAILED  error 알림 "차량은 정지되지 않았습니다"
          └ 그 외/예외      warning·error 알림
        → 실제 ESTOP 표시는 status 이벤트 수신 후에만
        → ESTOP 진입 시 버튼 비활성 + "해제 불가" 안내

[전체]  버튼 → confirm(대상 N대) → POST
          ├ failed=0        success
          ├ published=0     error
          └ 그 외           warning + 실패 차량 목록
```

### 2.4 상태 전이도

```
       ┌─────────────── 선택 없음 ──────────────┐
       │        (미니맵에서 차량 클릭)          │
       ▼                                        │
   차량 선택됨 ──── 적재 NORMAL/CAUTION ────────┤
       │                                        │
       ├──── WARNING ──→ 오버레이(주황) ────────┤
       ├──── DANGER  ──→ 오버레이(빨강) ────────┤
       │                                        │
       ├──── ESTOP 이벤트 ──→ 정지 상태 ────────┤
       │                     (해제 불가 안내)   │
       └──── STOMP 끊김 ──→ 연결 끊김 배너 ─────┘
                             (마지막 값 유지)
```

---

## 3. 데스크톱 메인 와이어프레임 (1920×1080 기준)

```
┌──────────────────────────────────────────────────────────────────────────────────────┐
│  디지털 트윈 실시간 관제                                                              │ 56
│  좌측 영상 · 우측 상세/미니맵 · 차량 상태·위치 실시간 연동                             │
├──────────────────────────────────────────────────────────────────────────────────────┤
│ ⚠ [알림 영역] 명령 결과 · 조회 실패 (없으면 렌더 안 함)                          [✕] │ 0~48
├───────────────────────────────────────────────┬──────────────────────────────────────┤
│                                               │ 차량 상세              Vehicle Detail│
│  ┌─ 디지털 트윈 영상 ─────────────────────┐  │ ┌──────────────────────────────────┐ │
│  │ 디지털 트윈 영상        [실시간 연결됨] │  │ │ FORKLIFT-01            [REAL]    │ │
│  │ Isaac Sim Stream        [영상 연결됨  ] │  │ │ REAL-F01                         │ │
│  │                         [⛶ 전체화면  ] │  │ ├──────────────────────────────────┤ │
│  │                    ┌──────────────────┐ │  │ │ ● 이동 중            10:32:14    │ │
│  │                    │ 선택 차량        │ │  │ ├──────────────────────────────────┤ │
│  │                    │ REAL-F01         │ │  │ │ 위치            [REAL · map]     │ │
│  │        (영상)      │ ● 이동 중        │ │  │ │ ┌─────────┬─────────┐            │ │
│  │                    │ 작업: TASK-0007  │ │  │ │ │X  12.40 │Y   8.15 │            │ │
│  │                    └──────────────────┘ │  │ │ ├─────────┼─────────┤            │ │
│  │                                         │  │ │ │H  90.0  │S   0.85 │            │ │
│  │                                         │  │ │ └─────────┴─────────┘            │ │
│  │  ┌───────────────────────────────────┐  │  │ ├──────────────────────────────────┤ │
│  │  │ [적재 경고 오버레이 — 조건부]     │  │  │ │ 적재 안전            [VISION]    │ │
│  │  └───────────────────────────────────┘  │  │ │ ┌──────────────────────────────┐ │ │
│  └─────────────────────────────────────────┘  │ │ │ ✓ 정상                       │ │ │
│                                               │ │ └──────────────────────────────┘ │ │
│                                               │ │ ┌────────┬────────┐              │ │
│                                               │ │ │포크0.86│화물1.42│              │ │
│                                               │ │ ├────────┼────────┤              │ │
│                                               │ │ │Roll 1.2│Pitch0.4│              │ │
│                                               │ │ ├────────┼────────┤              │ │
│                                               │ │ │편X-0.02│편Y 0.01│              │ │
│                                               │ │ └────────┴────────┘              │ │
│                                               │ │ CARGO-001        감지 10:32:11   │ │
│                                               │ ├──────────────────────────────────┤ │
│                                               │ │ 현재 작업                        │ │
│                                               │ │ TASK-0007 / TRANSPORTING         │ │
│                                               │ ├──────────────────────────────────┤ │
│                                               │ │ [       STOP  (미연결)        ]  │ │
│                                               │ │ [   REAL-F01 비상정지         ]  │ │
│                                               │ └──────────────────────────────────┘ │
│                                               ├──────────────────────────────────────┤
│                                               │ 미니맵                     3/3 대    │
│                                               │ ┌──────────────────────────────────┐ │
│                                               │ │  ▣ 랙 A      ▣ 랙 B              │ │
│                                               │ │        ◆F01                      │ │
│                                               │ │   ●F02          ●F03             │ │
│                                               │ │  ▣ 랙 C      ▣ 랙 D              │ │
│                                               │ └──────────────────────────────────┘ │
├───────────────────────────────────────────────┴──────────────────────────────────────┤
│ 🛑 비상 시 전체 차량을 즉시 정지시킵니다.   [대상 3대]        [ ⬛ 전체 정지 ]        │ 56
└──────────────────────────────────────────────────────────────────────────────────────┘
                          ◆ = 선택 차량   ● = 일반 차량
```

**레이아웃 골격**

```
Header            56px 고정
Notice            0 또는 auto (조건부)
Main    flex-1    grid-cols-[2fr_1fr]  gap-12
  ├ Left          영상 (전체 높이)
  └ Right         grid-rows-[minmax(0,1.15fr)_minmax(0,1fr)]
Global E-Stop     56px 고정 (하단)
```

---

## 4. 선택 차량이 없는 상태

```
┌───────────────────────────────────────────────┬──────────────────────────────────────┐
│  ┌─ 디지털 트윈 영상 ─────────────────────┐  │ 차량 상세              Vehicle Detail│
│  │ 디지털 트윈 영상        [실시간 연결됨] │  │ ┌──────────────────────────────────┐ │
│  │                                         │  │ │                                  │ │
│  │                                         │  │ │                                  │ │
│  │              (영상만 표시)              │  │ │   미니맵에서 차량을 선택하면     │ │
│  │                                         │  │ │   상세 정보가 표시됩니다.        │ │
│  │      ※ 선택 차량 오버레이 없음          │  │ │                                  │ │
│  │      ※ 적재 경고 오버레이 없음          │  │ │                                  │ │
│  │                                         │  │ └──────────────────────────────────┘ │
│  └─────────────────────────────────────────┘  ├──────────────────────────────────────┤
│                                               │ 미니맵                     3/3 대    │
│                                               │ │  ●F01   ●F02   ●F03            │ │
├───────────────────────────────────────────────┴──────────────────────────────────────┤
│ 🛑 비상 시 전체 차량을 즉시 정지시킵니다.   [대상 3대]        [ ⬛ 전체 정지 ]        │
└──────────────────────────────────────────────────────────────────────────────────────┘
```

- 전체 비상정지는 **선택과 무관하게 계속 활성** — 선택이 없어도 전체 정지는 가능해야 한다
- 차량이 0대면 미니맵 자리에 "등록된 활성 차량이 없습니다 [새로고침]"

---

## 5. 정상 적재 상태 (NORMAL)

```
우측 상단 · 적재 안전 영역
┌──────────────────────────────────────────────┐
│ 적재 안전                          [VISION]  │
│ ┌──────────────────────────────────────────┐ │
│ │ ✓  정상                                  │ │  ← 초록 테두리 + 체크 아이콘 + "정상"
│ └──────────────────────────────────────────┘ │
│ ┌──────────────┬──────────────┐              │
│ │포크 높이 0.86│화물 높이 1.42│              │
│ ├──────────────┼──────────────┤              │
│ │Roll(°)   1.2 │Pitch(°)  0.4 │              │
│ ├──────────────┼──────────────┤              │
│ │편향X   -0.02 │편향Y    0.01 │              │
│ └──────────────┴──────────────┘              │
│ CARGO-001                  감지 10:32:11     │
└──────────────────────────────────────────────┘

좌측 영상: 오버레이 없음
```

**CAUTION**은 동일 레이아웃에 배지만 `⚠ 주의`(호박색) — **오버레이는 뜨지 않는다.**

---

## 6. 화물 기울기 WARNING 상태

```
┌───────────────────────────────────────────────┬──────────────────────────────────────┐
│  ┌─ 디지털 트윈 영상 ─────────────────────┐  │ 적재 안전            [VISION]        │
│  │                                         │  │ ┌──────────────────────────────────┐ │
│  │                                         │  │ │ ⚠ 경고  [LOAD_TILT_EXCEEDED]     │ │ 주황
│  │              (영상)                     │  │ │ 화물이 좌측으로 과도하게         │ │
│  │                                         │  │ │ 기울었습니다.                    │ │
│  │                                         │  │ └──────────────────────────────────┘ │
│  │  ┌───────────────────────────────────┐  │  │ ┌────────┬────────┐                  │
│  │  │ ⚠  적재 경고  REAL-F01            │  │  │ │포크0.86│화물1.42│                  │
│  │  │    [LOAD_TILT_EXCEEDED]           │  │  │ ├────────┼────────┤                  │
│  │  │    화물이 좌측으로 과도하게       │  │  │ │Roll 7.4│Pitch3.1│ ← 값 강조         │
│  │  │    기울었습니다.                  │  │  │ ├────────┼────────┤                  │
│  │  └───────────────────────────────────┘  │  │ │편X-0.18│편Y 0.04│                  │
│  └─────────────────────────────────────────┘  │ └────────┴────────┘                  │
│                     ↑ 하단 중앙, 클릭 통과    │ CARGO-001      감지 10:32:11         │
│                                               │ [   REAL-F01 비상정지   ]  ← 활성    │
└───────────────────────────────────────────────┴──────────────────────────────────────┘
```

- 오버레이는 **하단 중앙**, `pointer-events-none` — 전체화면 버튼 등 조작을 막지 않는다
- `role="alert"` `aria-live="assertive"`
- **비상정지 버튼은 자동으로 눌리지 않는다.** 운영자 판단으로만 실행

---

## 7. DANGER 상태

```
│  │  ┌───────────────────────────────────┐  │  │ ┌──────────────────────────────────┐ │
│  │  │ 🛑  적재 위험  REAL-F01           │  │  │ │ 🛑 위험  [LOAD_UNSTABLE]         │ │ 빨강
│  │  │     [LOAD_UNSTABLE]               │  │  │ │ 화물이 불안정합니다. 즉시        │ │
│  │  │     화물이 불안정합니다.          │  │  │ │ 확인이 필요합니다.               │ │
│  │  │     즉시 확인이 필요합니다.       │  │  │ └──────────────────────────────────┘ │
│  │  └───────────────────────────────────┘  │  │
```

| WARNING | DANGER |
|---|---|
| 주황 계열 · `⚠` · "적재 경고" | 빨강 계열 · `🛑` · "적재 **위험**" |

**DANGER여도 ESTOP API를 자동 호출하지 않는다.** 시스템은 알리고, 정지 결정은 사람이 한다.
→ 이 정책은 운영자가 사전에 알아야 하므로 §11 하단 바 문구에 반영한다.

### UNKNOWN (판정 불가)

```
│ ┌──────────────────────────────────────────┐ │
│ │ ? 판정 불가                              │ │  회색 · "?" 아이콘
│ └──────────────────────────────────────────┘ │
```

- **오버레이 없음** — 판정 불가를 위험으로 승격하면 오탐이 쏟아진다
- 정상(초록)과 명확히 구분되는 회색 + "판정 불가" 텍스트

### 데이터 미수신 / 오래된 데이터

```
[미수신]  적재 안전 데이터 미수신 — 비전·센서에서 아직 전달된 값이 없습니다.

[오래됨]  ┌──────────────────────────────────────┐
          │ ⏱ 마지막 감지 이후 47초 경과 —       │  위험 단계는 그대로 두고
          │   최신 값이 아닐 수 있습니다.        │  신선도만 덧붙인다
          └──────────────────────────────────────┘
```

---

## 8. 개별 비상정지 확인 상태

```
[1] confirm                    ┌────────────────────────────────────┐
                               │ REAL-F01 차량에 비상정지 명령을    │
                               │ 전송하시겠습니까?                  │
                               │              [취소]  [확인]        │
                               └────────────────────────────────────┘
                                     ↓ 취소 → API 호출 없음

[2] 전송 중          │ [ ⬛ 명령 전송 중...        ] │  disabled, aria-busy=true

[3-A] PUBLISHED      │ ✓ REAL-F01 비상정지 명령 전송 완료                    [✕] │
                     │   commandId a1b2… · 실제 정지 여부는 차량 상태        │
                     │   이벤트로 확인됩니다.                                │

[3-B] PUBLISH_FAILED │ ✕ REAL-F01 비상정지 명령 발행 실패                    [✕] │
                     │   MQTT 브로커로 명령을 발행하지 못했습니다.           │
                     │   차량은 정지되지 않았습니다.                         │

[4] status 이벤트 도착 후 (실제 ESTOP)
    ┌──────────────────────────────────┐
    │ ● 비상정지            10:33:02   │  상태 배지 = ESTOP
    ├──────────────────────────────────┤
    │ [       STOP  (미연결)        ]  │
    │ [   ⬛ 비상정지 상태          ]  │  disabled
    │ ⓘ 이 화면에서는 비상정지를       │  ★ RESET_ESTOP 미지원 안내
    │   해제할 수 없습니다.            │
    └──────────────────────────────────┘
```

**핵심 원칙 3가지**

1. `HTTP 201`이 아니라 **응답 본문 `data.status`** 로 성공 판정 (발행 실패도 201로 온다)
2. 발행 성공만으로 상태를 ESTOP으로 바꾸지 **않는다** — status 이벤트가 유일한 근거
3. ESTOP 진입 시 **해제 불가를 UI에 명시** (현재 RESET_ESTOP 미구현)

---

## 9. 전체 비상정지 확인 상태

```
[1] confirm    ┌──────────────────────────────────────────────┐
               │ 활성 차량 3대에 비상정지 명령을 전송합니다.  │
               │ 계속하시겠습니까?          [취소]  [확인]    │
               └──────────────────────────────────────────────┘

[2] 전송 중    │ 🛑 …  [대상 3대]        [ ⬛ 전송 중... ] │  disabled

[3-A] 전체 성공  │ ✓ 전체 비상정지 명령 전송 완료 (3/3대)              [✕] │
                 │   실제 정지 여부는 각 차량 상태 이벤트로 확인됩니다. │

[3-B] 부분 실패  │ ⚠ 2대 전송 성공, 1대 발행 실패                      [✕] │
                 │   실패 차량: REAL-F03                                │

[3-C] 전체 실패  │ ✕ 전체 비상정지 명령 발행 실패 (3/3대)              [✕] │
                 │   실패 차량: REAL-F01, REAL-F02, REAL-F03            │

[4] 대상 0대     │ 🛑 …  [대상 0대]   [ ⬛ 전체 정지 ] disabled          │
                 │ title="대상 활성 차량이 없습니다"                     │
```

**부분 실패를 성공으로 표시하지 않는다** — 이것이 이 화면에서 가장 중요한 표시 규칙이다.

---

## 10. 연결 끊김 상태

```
┌──────────────────────────────────────────────────────────────────────────────────────┐
│ ⚠ 실시간 연결이 끊겼습니다. 표시된 값은 마지막 수신 시점 기준입니다.   [다시 시도]  │
├───────────────────────────────────────────────┬──────────────────────────────────────┤
│  │ 디지털 트윈 영상    [⚠ 실시간 연결 끊김]  │  │ 차량 상세                          │
│  │                     [영상 연동 대기·Mock] │  │  ※ 마지막 수신 값 유지             │
│  │            (영상 유지)                    │  │  ※ 값을 지우지 않는다              │
│  └───────────────────────────────────────────┘  │  ⏱ 마지막 감지 이후 2분 경과       │
└───────────────────────────────────────────────┴──────────────────────────────────────┘
```

**연결 상태 2축을 분리 표시** (현재 코드도 배지 2개로 분리돼 있다)

| 축 | 배지 | 값 |
|---|---|---|
| 관제 실시간 (STOMP) | `RealtimeConnectionBadge` | 연결 중 / 연결됨 / 연결 끊김 / 연결 실패 |
| 영상 스트림 | `ConnectionStatusBadge` | 영상 연동 대기·Mock / 연결 중 / 연결됨 / 실패 |

**끊김 시 규칙**

- 마지막 수신 값을 **지우지 않는다**(빈 화면이 더 위험하다)
- 대신 "언제 것인지"를 명시 — stale 표시가 이때 결정적 역할을 한다
- 비상정지 버튼은 **계속 활성** — REST는 WebSocket과 독립 경로다
- 재연결되면 dashboard REST 재조회(2번째 연결부터)

**초기 로딩 실패**

```
│ ⚠ 백엔드에 연결할 수 없습니다. 서버가 실행 중인지 확인해 주세요.     [다시 시도]  │
```

---

## 11. 영역별 표시 데이터와 버튼 동작 정의

### 11.1 Header

| 영역 | 표시 내용 | 데이터 출처 | 사용자 동작 | 상태 변화 |
|---|---|---|---|---|
| 제목 | "디지털 트윈 실시간 관제" + 부제 | 정적 | 없음 | 없음 |
| dev 영상상태 | 영상 상태 셀렉트 | 로컬 state | 선택 | `streamStatus` 변경 (**개발 전용, 운영 제거 대상**) |

### 11.2 알림 영역

| 영역 | 표시 내용 | 데이터 출처 | 사용자 동작 | 상태 변화 |
|---|---|---|---|---|
| 대시보드 오류 | 조회 실패 메시지 | `errorMessage` | [다시 시도] | `loadDashboard()` 재호출 |
| 명령 알림 | tone(success/warning/error) + 제목 + 상세 | 비상정지 응답 | [✕] | `notice = null` |

### 11.3 좌측 — 디지털 트윈 영상

| 영역 | 표시 내용 | 데이터 출처 | 사용자 동작 | 상태 변화 |
|---|---|---|---|---|
| 영상 레이어 | 스트림(현재 placeholder) | `DigitalTwinVideoLayer` | 없음 | `streamStatus` 따라 dim |
| 실시간 배지 | 연결 중/연결됨/끊김/실패 | `useMonitoringSocket` | 없음 | STOMP 상태 |
| 영상 배지 | 대기·Mock/연결 중/연결됨/실패 | `streamStatus` | 없음 | — |
| 전체화면 | ⛶ 아이콘 | `document.fullscreenElement` | 클릭 | 전체화면 토글 |
| 선택 차량 오버레이 | vehicleId·상태·작업·source | `selectedVehicle` | 없음 | 선택/상태 변경 시 |
| **적재 경고 오버레이** | 아이콘·"경고/위험"·차량·riskCode·메시지 | `selectedLoadSafety` | **없음(클릭 통과)** | `riskLevel ∈ {WARNING, DANGER}` 일 때만 렌더 |

### 11.4 우측 상단 — 차량 상세

| 영역 | 표시 내용 | 데이터 출처 | 사용자 동작 | 상태 변화 |
|---|---|---|---|---|
| 헤더 | name·vehicleId·source·비활성 뱃지 | `DashboardVehicle` | 없음 | — |
| 상태 | 점 + 한글 라벨 + 갱신 시각 | `/topic/vehicles/status` | 없음 | status 이벤트 |
| 위치 | X·Y·Heading·Speed·생성/수신 시각 | `/topic/vehicles/location` | 없음 | location 이벤트 |
| **적재 안전 배지** | 아이콘 + 단계 라벨 + riskCode + 메시지 | `/topic/vehicles/load-safety` | 없음 | load-safety 이벤트 |
| **적재 측정값** | 포크·화물 높이, Roll, Pitch, 편향 X/Y | 동일 | 없음 | 동일 |
| **적재 신선도** | "N초 경과" (임계 초과 시) | `detectedAt` | 없음 | 시간 경과 |
| 현재 작업 | taskId·작업상태·명령상태 | dashboard | 없음 | — |
| **STOP** | "STOP" | — | 클릭 | **현재 미연결**(§12 P1) |
| **EMERGENCY STOP** | "{id} 비상정지" / "명령 전송 중..." / "비상정지 상태" | — | confirm → POST | pending → 알림 → (status 이벤트 시) disabled |
| **해제 불가 안내** | "이 화면에서는 해제할 수 없습니다" | 정적 | 없음 | ESTOP일 때만 표시 |

### 11.5 우측 하단 — 미니맵

| 영역 | 표시 내용 | 데이터 출처 | 사용자 동작 | 상태 변화 |
|---|---|---|---|---|
| 배경 | 랙·통로 구조 | 정적 | 없음 | — |
| 차량 마커 | 상태색 + 방향 + ID | `location` + `status` | **클릭 → 선택**(팝업 없음) | `selectedVehicleId` 변경 |
| 표시 수 | "N/M 대" | 렌더 가능 차량 수 | 없음 | 위치 수신 시 |

- 위치 없는 차량은 **표시하지 않는다**
- STOP·배터리 표시 없음

### 11.6 하단 — 전체 비상정지

| 영역 | 표시 내용 | 데이터 출처 | 사용자 동작 | 상태 변화 |
|---|---|---|---|---|
| 안내 문구 | "비상 시 전체 차량을 즉시 정지시킵니다." | 정적 | 없음 | — |
| 대상 수 | "대상 N대" | `vehicles.filter(active)` | 없음 | 목록 변경 시 |
| 버튼 | "전체 정지" / "전송 중..." | — | confirm → POST | pending → 알림 |

> **권장 문구 추가**: "위험 감지 시에도 자동으로 정지하지 않습니다 — 정지는 운영자가 실행합니다."
> DANGER 자동 정지가 없다는 정책을 운영자가 **사전에** 알아야 한다.

### 11.7 상태 표기 규칙 (색상 의존 금지)

| 단계 | 아이콘 | 텍스트 | 색 | 오버레이 |
|---|---|---|---|---|
| NORMAL | ✓ | 정상 | 초록 | ✗ |
| CAUTION | ⚠ | 주의 | 호박 | ✗ |
| WARNING | ⚠ | 경고 | 주황 | **✓** |
| DANGER | 🛑 | 위험 | 빨강 | **✓** |
| UNKNOWN | ? | 판정 불가 | 회색 | ✗ |

**모든 상태는 아이콘 + 텍스트를 함께 제공한다.** 색맹 사용자와 흑백 출력에서도 구분 가능해야 한다.
차량 상태 10종도 동일 — 미니맵 마커는 색 + ID 라벨, 상세 패널은 점 + 한글 라벨.

---

## 12. 구현 우선순위

### P0 — 안전상 즉시

| # | 작업 | 근거 |
|---|---|---|
| 1 | **ESTOP 해제 불가 안내 문구** | 현재 RESET_ESTOP 미구현이라 화면에서 되돌릴 수 없다. 운영자가 모르고 누르면 복구 수단이 없다 |
| 2 | **"자동 정지 없음" 안내** | DANGER를 봐도 시스템은 멈추지 않는다는 사실을 사전에 알려야 한다 |

### P1 — 통합 전

| # | 작업 | 근거 |
|---|---|---|
| 3 | **전체 비상정지 바를 하단으로 이동** | prompt64 확정 구조와 현재 코드의 유일한 차이 |
| 4 | **STOP 버튼 처리 결정** | 현재 클릭해도 아무 반응 없음. API 연결 또는 disabled + "준비 중" |
| 5 | 적재 안전 신선도 임계값 확정 | 현재 15초는 센서 주기 미확정 상태의 잠정값 |
| 6 | dev 영상상태 셀렉트 제거 | 운영 화면에 개발 전용 컨트롤 노출 |

### P2 — 운영 전

| # | 작업 |
|---|---|
| 7 | 미니맵 마커에 적재 위험 표식(선택 차량 외 위험 차량 인지) |
| 8 | `window.confirm` → 프로젝트 스타일 모달(문구 강조·reason 입력 겸용) |
| 9 | 차량 OFFLINE 시 적재 안전 표시 정책 |
| 10 | 명령 결과(`/topic/vehicles/result`) 구독 — "실제로 멈췄는지" 표시 |

### P3 — 개선

| # | 작업 |
|---|---|
| 11 | 위험 차량 목록 요약(다중 차량 동시 위험) |
| 12 | 적재 기울기 시각화(수평계 형태) |
| 13 | 알림 히스토리 |

---

## 13. Figma 이관 가이드

### 13.1 프레임·그리드

```
Frame: Desktop / 1920 × 1080
Padding: 12
Gap: 12
Auto Layout: Vertical
```

| 영역 | 높이 | 비율 | 비고 |
|---|---|---|---|
| Header | 56 | 고정 | |
| Notice | 0 / 48 | 조건부 | 없으면 렌더 안 함 |
| Main | flex-1 | ~88% | |
| Global E-Stop | 56 | 고정 | 하단 |

**Main 가로 분할**

| 영역 | 비율 | 1920 기준 |
|---|---|---|
| 좌측 영상 | **2fr** | 약 1264 |
| 우측 컬럼 | **1fr** | 약 632 |

**우측 세로 분할** — 적재 안전 6필드가 들어가 상단을 약간 크게 잡는다

| 영역 | 비율 |
|---|---|
| 차량 상세 | **1.15fr** |
| 미니맵 | **1fr** |

### 13.2 계층 구조

```
Page
├─ Header                        [Auto H, Space Between]
├─ NoticeStack                   [Auto V, Gap 8]  (조건부)
├─ Main                          [Grid 2fr 1fr, Gap 12]
│  ├─ VideoPanel                 [Stack, Radius 8, Border]
│  │  ├─ VideoLayer              (fill)
│  │  ├─ TopLeftLabel            (abs: 12,12)
│  │  ├─ TopRightControls        (abs: r12,t12) [Auto H, Gap 8]
│  │  ├─ SelectedVehicleCard     (abs: r12, t44) [W 208]
│  │  └─ LoadSafetyOverlay       (abs: bottom 12, center) ★조건부
│  └─ RightColumn                [Grid rows 1.15fr 1fr, Gap 12]
│     ├─ VehicleDetailPanel      [Auto V, Gap 12, Radius 8]
│     │  ├─ PanelHeader
│     │  ├─ VehicleIdentity
│     │  ├─ StatusRow
│     │  ├─ LocationGrid         [2 cols]
│     │  ├─ LoadSafetySection ★  [Auto V, Gap 8]
│     │  │  ├─ RiskBadge
│     │  │  ├─ StaleNotice       (조건부)
│     │  │  ├─ MeasurementGrid   [2 cols × 3 rows]
│     │  │  └─ CargoFooter
│     │  ├─ CurrentTask
│     │  └─ ControlButtons       [Auto V, Gap 8]
│     └─ MiniMapPanel
└─ GlobalEStopBar                [Auto H, Space Between]
```

### 13.3 디자인 토큰 (현재 코드 값)

**배경·표면**

| 토큰 | 값 | 용도 |
|---|---|---|
| `bg/app` | `#020617` (slate-950) | 페이지 |
| `bg/panel` | `#0b1220` | 패널 |
| `bg/subtle` | `rgba(255,255,255,0.05)` | 필드 |
| `border/default` | `#334155` (slate-700) | 패널 테두리 |

**위험 단계** — 배경은 `950/40~50`, 테두리는 `500/30~50` 패턴

| 단계 | 주색 | 텍스트 |
|---|---|---|
| NORMAL | emerald-500 `#10b981` | emerald-100 |
| CAUTION | amber-500 `#f59e0b` | amber-100 |
| WARNING | orange-500 `#f97316` | orange-100 |
| DANGER | red-500 `#ef4444` | red-100 |
| UNKNOWN | slate-500 `#64748b` | slate-200 |

**차량 상태 10종** — `components/monitoring/vehicle-status.ts` 값 그대로

| 상태 | base | 상태 | base |
|---|---|---|---|
| MOVING | `#10b981` | UNLOADING | `#fb923c` |
| IDLE | `#3b82f6` | ESTOP | `#ef4444` |
| ACTIVE | `#eab308` | ERROR | `#dc2626` |
| LIFTING | `#a855f7` | OFFLINE | `#64748b` |
| LOADING | `#f59e0b` | UNKNOWN | `#64748b` |

**타이포** (Geist Sans / Geist Mono)

| 역할 | 크기 | 굵기 |
|---|---|---|
| 페이지 제목 | 16 | 600 |
| 패널 제목 | 12 | 600 |
| 본문 | 12 | 400 |
| 라벨(대문자) | 10 | 500 |
| 수치(mono) | 12 | 400 |
| 오버레이 제목 | 14 | 700 |
| 버튼 | 14 | 600 |

**간격·모서리**: 패널 radius 8 / 필드·배지 6 / 패널 padding 12 / 요소 gap 8~12

### 13.4 Figma 컴포넌트 Variants

```
RiskBadge          variant = NORMAL | CAUTION | WARNING | DANGER | UNKNOWN
LoadSafetyOverlay  variant = WARNING | DANGER          (+ hidden)
VehicleMarker      status  = 10종  ×  selected = true | false
ConnectionBadge    type    = realtime | stream  ×  state = 4종
EStopButton        state   = default | pending | disabled-estop
Notice             tone    = success | warning | error
LoadSafetySection  state   = data | empty | stale
```

### 13.5 이관 시 주의

1. **오버레이는 조건부** — WARNING/DANGER에서만. 항상 보이게 그리면 안 된다
2. **아이콘 없이 색만 쓰지 말 것** — 모든 상태 배지는 아이콘+텍스트 필수
3. **미측정 값은 `—`** — 0으로 그리면 "기울기 0"과 "측정 안 됨"이 섞인다
4. **STOP은 현재 미연결** — 시안에 활성 버튼으로 그리면 구현된 것으로 오해된다
5. **ESTOP 상태에는 해제 불가 안내가 반드시 붙는다**

---

## 부록. 이 문서가 근거한 실제 파일

| 영역 | 파일 |
|---|---|
| 레이아웃 | `app/page.tsx` |
| 영상 | `MainRealtimeMonitoringView.tsx`, `DigitalTwinVideoLayer.tsx` |
| 배지 | `RealtimeConnectionBadge.tsx`, `ConnectionStatusBadge.tsx` |
| 차량 상세 | `VehicleDetailPanel.tsx` |
| 적재 안전 | `LoadSafetyPanel.tsx`, `LoadSafetyOverlay.tsx`, `types/loadSafety.ts`, `lib/loadSafety.ts` |
| 미니맵 | `MiniMap.tsx`, `MiniMapVehicleMarker.tsx`, `lib/coordinate.ts` |
| 비상정지 | `GlobalEmergencyStopBar.tsx`, `CommandNotice.tsx`, `lib/api/commandApi.ts` |
| 상태 토큰 | `components/monitoring/vehicle-status.ts` |
| 실시간 | `hooks/useMonitoringSocket.ts`, `hooks/useMonitoringDashboard.ts` |
