# 관제 화면 최종 와이어프레임

> 대상: `frontend/monitoring-control-system` (Next.js 16 App Router · React 19 · TS · Tailwind v4 · SockJS/STOMP)
> 기준일: 2026-07-29 · **실제 코드 검증 기준**
> 선행 문서: `wireframe-monitoring.md`(유지, 삭제하지 않음) → 이 문서가 구현용 최종본
> 코드 수정·Git 명령·서버 실행·API 호출 **없음**

---

## 1. 현재 화면 분석

### 1.1 컴포넌트 트리 (실제 파일)

```
app/page.tsx  (MonitoringPage, "use client")
├─ header                          제목 + ⚠dev 전용 영상상태 셀렉트
├─ DashboardErrorBanner            loadState==="error" 조건부
├─ CommandNotice                   명령 결과 알림 (단일 슬롯 — 덮어쓰기)
├─ GlobalEmergencyStopBar          ⚠현재 상단 배치
└─ grid lg:[2fr_1fr]
   ├─ MainRealtimeMonitoringView
   │  ├─ DigitalTwinVideoLayer     placeholder(샘플 스틸)
   │  ├─ RealtimeConnectionBadge   STOMP 4상태
   │  ├─ ConnectionStatusBadge     영상 4상태
   │  ├─ FullscreenButton
   │  ├─ SelectedVehicleOverlay
   │  ├─ overlay 슬롯 → LoadSafetyOverlay   WARNING/DANGER만
   │  └─ 연결중/실패 오버레이 (z-30)
   └─ grid-rows-2
      ├─ VehicleDetailPanel
      │  ├─ 상태 / 위치
      │  ├─ LoadSafetyPanel
      │  ├─ 현재 작업
      │  └─ STOP · EMERGENCY STOP
      └─ MiniMap → MiniMapVehicleMarker[]
```

### 1.2 데이터 계약 (검증됨)

| 경로 | 종류 | 용도 |
|---|---|---|
| `GET /api/monitoring/dashboard` | REST | 차량·작업 초기 상태 |
| `GET /api/vehicles/load-safety/latest` | REST | 적재 안전 초기 상태 |
| `POST /api/vehicles/{id}/commands/emergency-stop` | REST | 개별 비상정지 |
| `POST /api/vehicles/commands/emergency-stop-all` | REST | 전체 비상정지 |
| `/topic/vehicles/status` | STOMP | **실제 ESTOP 판정의 유일한 근거** |
| `/topic/vehicles/location` | STOMP | 마커 이동 |
| `/topic/vehicles/load-safety` | STOMP | riskLevel 갱신 |
| `/topic/vehicles/result` | STOMP | **존재하나 미구독** |

### 1.3 §2 지정 파일 검증 결과

| 지정 파일 | 실제 |
|---|---|
| `types/vehicle.ts` | **존재하지 않는다.** 차량 타입은 `types/monitoring.ts`의 `DashboardVehicle`·`VehicleStatus`가 담당 |
| 나머지 16개 | 전부 존재, 분석 완료 |

### 1.4 키워드 검색 결과 (핵심만)

| 키워드 | 결과 |
|---|---|
| `console.log` | **1건** — `app/page.tsx:306` `onStop={(id) => console.log("[monitoring] stop (미연결)", id)}` |
| `RESET_ESTOP` | 프론트 **타입 정의에만** 존재(`types/command.ts:17` `SafetyCommandName`). API·버튼·핸들러 전무 |
| `placeholder` | `DigitalTwinVideoLayer` — 샘플 스틸 이미지 |
| `Mock` | `ConnectionStatusBadge` idle 라벨 "영상 연동 대기 · Mock" |
| `riskLevel` | 프론트 어디에도 **roll/pitch 임계값 비교 코드 없음** — 표시만 |

---

## 2. 구현·미구현 구분

| 기능 | 상태 | 근거 |
|---|---|---|
| 차량 상태 실시간 | **구현 완료** | `/topic/vehicles/status` → `applyStatusEvent` |
| 차량 위치 실시간 | **구현 완료** | `/topic/vehicles/location` → 마커 이동 |
| 미니맵 선택 | **구현 완료** | 팝업 없이 `onSelectVehicle` |
| 선택 차량 상세 | **구현 완료** | |
| 적재 안전 패널 | **구현 완료** | 5단계 배지 + 6측정값 + stale |
| 적재 안전 오버레이 | **구현 완료** | WARNING/DANGER 조건부 |
| 개별 비상정지 | **구현 완료** | confirm·pending·3분기 알림 |
| 전체 비상정지 | **구현 완료** | 부분 실패 구분 |
| 연결 상태 배지 | **구현 완료** | STOMP·영상 2축 분리 |
| **STOP 버튼** | **미연결** | `console.log`만. 클릭해도 무반응·무피드백 |
| **디지털 트윈 영상** | **Placeholder** | 샘플 스틸. 스트림 프로토콜 미확정 |
| **영상 상태 셀렉트** | **Mock (dev 전용)** | 운영 화면에 노출 중 |
| **적재 안전 데이터** | **데이터 계약 대기** | 수신 준비 완료, **발행 주체 없음** |
| **적재 stale 임계값** | **데이터 계약 대기** | 15초는 센서 주기 미확정 상태의 잠정값 |
| **RESET_ESTOP** | **미구현** | 화면에서 ESTOP 해제 불가 |
| **미니맵 위험 링** | **미구현** | 마커가 loadSafety를 **prop으로 받지도 않음** |
| **위험 차량 요약** | **미구현** | |
| **명령 결과 표시** | **미구현** | `/topic/vehicles/result` 미구독 |
| **알림 스택** | **부분 구현** | 단일 슬롯 — 새 알림이 이전 것을 덮어씀 |

---

## 3. 최종 화면 구조

### 3.1 확정 구조 대조

| 확정 항목 | 현재 코드 | 최종안 |
|---|---|---|
| 좌측 대형 = 영상 | ✅ 2fr | 유지 |
| 우측 상단 = 차량 상세 | ✅ | 유지 |
| 우측 하단 = 미니맵 | ✅ | 유지 |
| **하단 = 전체 비상정지** | ❌ **상단 배치** | **하단으로 이동** |
| 사이드바 없음 | ✅ | 유지 |
| 2D/3D 토글 없음 | ✅ | 유지 |
| 미니맵 팝업 없음 | ✅ | 유지 |
| 배터리 없음 | ✅ | 유지 |

**차이 1건**: 전체 비상정지 바가 코드에서는 관제 본문 **앞**에 있다. 확정 구조는 "하단"이다.

### 3.2 추가 영역 — 위험 차량 요약

확정 구조를 깨지 않는 위치로 **헤더 아래 · 알림 영역과 같은 계열 슬롯**을 제안한다.

- 조건부 렌더 — 위험 차량 0대면 **DOM에서 사라진다** → 평상시 레이아웃은 확정안과 **완전히 동일**
- 사이드바·토글·팝업 어느 것도 추가하지 않는다
- 좌우 4분할 구조를 건드리지 않는다

### 3.3 최종 골격

```
Header                    56px
[위험 차량 요약]          조건부 (0대면 없음)
[명령 결과 알림]          조건부
Main  flex-1  grid-cols-[2fr_1fr]  gap-12
  ├ 영상
  └ grid-rows-[1.15fr_1fr]
     ├ 차량 상세
     └ 미니맵
Global E-Stop             56px  ← 하단
```

---

## 4. 메인 데스크톱 와이어프레임 (1920×1080)

```
┌──────────────────────────────────────────────────────────────────────────────────────┐
│ 디지털 트윈 실시간 관제                                                               │ 56
│ 좌측 영상 · 우측 상세/미니맵 · 실시간 연동                                            │
├──────────────────────────────────────────────────────────────────────────────────────┤
│ ⚠ 위험 차량 2대   [🛑 SIM-F03 · LOAD_UNSTABLE]  [⚠ REAL-F01 · LOAD_TILT_EXCEEDED]   │ 조건부
├──────────────────────────────────────────────────────────────────────────────────────┤
│ ✓ REAL-F01 비상정지 명령 전송 완료                                              [✕] │ 조건부
├───────────────────────────────────────────────┬──────────────────────────────────────┤
│ ┌─ 디지털 트윈 영상 ──────────────────────┐  │ 차량 상세              Vehicle Detail│
│ │ 디지털 트윈 영상       [⬤ 실시간 연결됨]│  │ ┌──────────────────────────────────┐ │
│ │ Isaac Sim Stream       [◐ 영상 대기·Mock]│  │ │ FORKLIFT-01              [REAL]  │ │
│ │                        [⛶            ]│  │ │ REAL-F01                         │ │
│ │                   ┌───────────────────┐ │  │ ├──────────────────────────────────┤ │
│ │                   │ 선택 차량         │ │  │ │ ● 이동 중            10:32:14    │ │
│ │      (영상)       │ REAL-F01          │ │  │ ├──────────────────────────────────┤ │
│ │                   │ ● 이동 중         │ │  │ │ 위치             [REAL · map]    │ │
│ │                   │ 작업: TASK-0007   │ │  │ │ ┌────────┬────────┐              │ │
│ │                   └───────────────────┘ │  │ │ │X 12.40 │Y  8.15 │              │ │
│ │                                         │  │ │ ├────────┼────────┤              │ │
│ │                                         │  │ │ │H  90.0 │S  0.85 │              │ │
│ │ ┌─────────────────────────────────────┐ │  │ │ └────────┴────────┘              │ │
│ │ │ ⚠ 적재 경고  REAL-F01               │ │  │ ├──────────────────────────────────┤ │
│ │ │   [LOAD_TILT_EXCEEDED]              │ │  │ │ 적재 안전            [VISION]    │ │
│ │ │   화물이 좌측으로 기울었습니다.     │ │  │ │ ┌──────────────────────────────┐ │ │
│ │ └─────────────────────────────────────┘ │  │ │ │ ⚠ 경고 [LOAD_TILT_EXCEEDED]  │ │ │
│ └─────────────────────────────────────────┘  │ │ │ 화물이 좌측으로 기울었습니다.│ │ │
│                                               │ │ └──────────────────────────────┘ │ │
│                                               │ │ ┌────────┬────────┐              │ │
│                                               │ │ │포크0.86│화물1.42│              │ │
│                                               │ │ ├────────┼────────┤              │ │
│                                               │ │ │Roll 7.4│Pitch3.1│              │ │
│                                               │ │ ├────────┼────────┤              │ │
│                                               │ │ │편X-0.18│편Y 0.04│              │ │
│                                               │ │ └────────┴────────┘              │ │
│                                               │ │ CARGO-001      감지 10:32:11     │ │
│                                               │ ├──────────────────────────────────┤ │
│                                               │ │ 현재 작업                        │ │
│                                               │ │ TASK-0007 / TRANSPORTING         │ │
│                                               │ ├──────────────────────────────────┤ │
│                                               │ │ ⊘ STOP 준비 중          disabled │ │
│                                               │ │   일반 정지 기능이 아직           │ │
│                                               │ │   연결되지 않았습니다.            │ │
│                                               │ │ [ 🛑 REAL-F01 비상정지        ]  │ │
│                                               │ └──────────────────────────────────┘ │
│                                               ├──────────────────────────────────────┤
│                                               │ 미니맵                     3/3 대    │
│                                               │ ┌──────────────────────────────────┐ │
│                                               │ │ ▣랙A          ▣랙B               │ │
│                                               │ │      ⓘ◆F01                       │ │
│                                               │ │   ●F02        ⓘ▣F03              │ │
│                                               │ │ ▣랙C          ▣랙D               │ │
│                                               │ └──────────────────────────────────┘ │
├───────────────────────────────────────────────┴──────────────────────────────────────┤
│ 🛑 비상 시 전체 차량을 즉시 정지시킵니다.  [대상 3대]         [ ⬛ 전체 정지 ]        │ 56
│    위험 감지 시에도 자동으로 정지하지 않습니다 — 정지는 운영자가 실행합니다.          │
└──────────────────────────────────────────────────────────────────────────────────────┘
       ◆=선택  ●=REAL  ▣=SIM  ⓘ=위험 링+배지
```

---

## 5. 차량 미선택 상태 (상태 1)

```
│ ┌─ 디지털 트윈 영상 ─────────────┐  │ 차량 상세                       │
│ │  ※선택차량 오버레이 없음       │  │ ┌─────────────────────────────┐ │
│ │  ※적재 경고 오버레이 없음      │  │ │  미니맵에서 차량을 선택하면 │ │
│ │        (영상만)                │  │ │  상세 정보가 표시됩니다.    │ │
│ └────────────────────────────────┘  │ └─────────────────────────────┘ │
├─────────────────────────────────────────────────────────────────────┤
│ 🛑 …  [대상 3대]                          [ ⬛ 전체 정지 ]  ← 활성   │
```

| 항목 | 내용 |
|---|---|
| **문구** | "미니맵에서 차량을 선택하면 상세 정보가 표시됩니다." |
| **활성** | 전체 정지, 미니맵 마커, 전체화면 |
| **비활성** | — (STOP·개별 비상정지는 **렌더 자체가 안 됨**) |
| **데이터 출처** | `selectedVehicleId === null` |
| **사용자 행동** | 마커 클릭 / 위험 요약 항목 클릭 |
| **다음 전이** | → 상태 2~7 (선택 차량의 riskLevel에 따라) |

> **전체 비상정지는 선택과 무관하게 항상 활성**이다 — 선택이 없어도 전체 정지는 가능해야 한다.
> 차량 0대면 미니맵 자리에 "등록된 활성 차량이 없습니다 [새로고침]", 전체 정지는 `disabled`.

---

## 6. 정상 상태 (상태 2 — NORMAL)

```
│ 적재 안전                            [VISION] │
│ ┌───────────────────────────────────────────┐ │
│ │ ✓  정상                                   │ │  초록
│ └───────────────────────────────────────────┘ │
│ ┌────────┬────────┐                           │
│ │포크0.86│화물1.42│                           │
│ ├────────┼────────┤                           │
│ │Roll 1.2│Pitch0.4│                           │
│ ├────────┼────────┤                           │
│ │편X-0.02│편Y 0.01│                           │
│ └────────┴────────┘                           │
│ CARGO-001              감지 10:32:11          │
                                    영상: 오버레이 없음
```

| 항목 | 내용 |
|---|---|
| **문구** | 배지 "정상" · 아이콘 `✓` |
| **활성** | 개별 비상정지, 전체 정지 |
| **비활성** | STOP(준비 중) |
| **데이터 출처** | `/topic/vehicles/load-safety` → `riskLevel: "NORMAL"` |
| **사용자 행동** | 관찰 |
| **다음 전이** | → CAUTION/WARNING/DANGER (이벤트) · → stale (시간 경과) |

---

## 7. CAUTION 상태 (상태 3)

```
│ ┌───────────────────────────────────────────┐ │
│ │ ⚠  주의   [LOAD_TILT_NEAR_LIMIT]          │ │  호박
│ │    화물 기울기가 기준에 근접했습니다.     │ │
│ └───────────────────────────────────────────┘ │
                                    영상: 오버레이 없음  ★
미니맵: 마커에 주의 링 + aria-label "주의"
```

| 항목 | 내용 |
|---|---|
| **문구** | "주의" + riskCode + 메시지 |
| **활성** | 개별 비상정지, 전체 정지 |
| **비활성** | STOP |
| **데이터 출처** | `riskLevel: "CAUTION"` |
| **사용자 행동** | 관찰 · 필요 시 수동 정지 |
| **다음 전이** | → NORMAL / WARNING / DANGER |

> ★ **CAUTION은 오버레이를 띄우지 않는다.** 주의 단계까지 영상을 가리면 운영자가 오버레이 자체를
> 무시하게 되어, 정작 WARNING/DANGER를 놓친다. 미니맵 링으로만 알린다.

---

## 8. WARNING 상태 (상태 4)

```
│ ┌─ 영상 ──────────────────────────────────┐ │ ┌──────────────────────────────┐ │
│ │                                         │ │ │ ⚠ 경고 [LOAD_TILT_EXCEEDED]  │ │ 주황
│ │ ┌─────────────────────────────────────┐ │ │ │ 화물이 좌측으로 과도하게     │ │
│ │ │ ⚠  적재 경고   REAL-F01             │ │ │ │ 기울었습니다.                │ │
│ │ │    [LOAD_TILT_EXCEEDED]             │ │ │ └──────────────────────────────┘ │
│ │ │    화물이 좌측으로 과도하게         │ │ │ │Roll 7.4│Pitch3.1│ ← 강조       │
│ │ │    기울었습니다.                    │ │ │ [ 🛑 REAL-F01 비상정지 ] ← 활성 │
│ │ └─────────────────────────────────────┘ │ │
│ └─────────────────────────────────────────┘ │
        ↑ 하단 중앙 · pointer-events-none
```

| 항목 | 내용 |
|---|---|
| **문구** | 오버레이 "적재 경고" + 차량ID + riskCode + 메시지 |
| **활성** | 개별 비상정지, 전체 정지 (오버레이는 클릭 통과) |
| **비활성** | STOP |
| **데이터 출처** | `riskLevel: "WARNING"` |
| **사용자 행동** | 원인 확인 → **운영자 판단으로** 비상정지 |
| **다음 전이** | → DANGER / CAUTION / NORMAL · → ESTOP(운영자 실행 시) |

> **자동 ESTOP 호출 없음.** 시스템은 알리고, 정지 결정은 사람이 한다.

---

## 9. DANGER 상태 (상태 5)

```
│ │ ┌─────────────────────────────────────┐ │ │ ┌──────────────────────────────┐ │
│ │ │ 🛑  적재 위험   SIM-F03             │ │ │ │ 🛑 위험 [LOAD_UNSTABLE]      │ │ 빨강
│ │ │     [LOAD_UNSTABLE]                 │ │ │ │ 화물이 불안정합니다.         │ │
│ │ │     화물이 불안정합니다.            │ │ │ │ 즉시 확인이 필요합니다.      │ │
│ │ │     즉시 확인이 필요합니다.         │ │ │ └──────────────────────────────┘ │
│ │ └─────────────────────────────────────┘ │ │
```

| | WARNING | DANGER |
|---|---|---|
| 아이콘 | `⚠` | `🛑` |
| 라벨 | 적재 경고 | 적재 **위험** |
| 색 | 주황 | 빨강 |
| 테두리 | 1px | **2px + 저강도 펄스**(reduced-motion 시 정지) |

| 항목 | 내용 |
|---|---|
| **활성** | 개별 비상정지(강조), 전체 정지 |
| **비활성** | STOP |
| **데이터 출처** | `riskLevel: "DANGER"` |
| **다음 전이** | → 운영자 비상정지 → ESTOP · → WARNING/NORMAL(개선 시) |

> **DANGER여도 ESTOP API를 자동 호출하지 않는다.** 이 정책은 운영자가 **사전에** 알아야 하므로
> 하단 바에 상시 문구를 둔다(§4 참조).

---

## 10. 미수신 · stale 상태 (상태 6·7)

### 10.1 데이터 미수신 (상태 6)

```
│ 적재 안전                                     │
│ ┌───────────────────────────────────────────┐ │
│ │ 적재 안전 데이터 미수신 —                 │ │  회색 · 배지 없음
│ │ 비전·센서에서 아직 전달된 값이 없습니다.  │ │
│ └───────────────────────────────────────────┘ │
                          영상: 오버레이 없음
                          미니맵: 미수신 표시(§16.10)
```

| 항목 | 내용 |
|---|---|
| **문구** | "적재 안전 데이터 미수신 — 비전·센서에서 아직 전달된 값이 없습니다." |
| **데이터 출처** | `selectedLoadSafety === null` |
| **다음 전이** | → 첫 이벤트 수신 시 해당 riskLevel 상태로 |

> **미수신을 "정상(초록)"으로 표시하지 않는다.** 이것이 이 화면에서 가장 위험한 혼동이다 —
> 센서가 죽었는데 초록 체크가 떠 있으면 운영자는 안전하다고 믿는다.

### 10.2 stale (상태 7)

```
│ ┌───────────────────────────────────────────┐ │
│ │ ⚠ 경고  [LOAD_TILT_EXCEEDED]              │ │  ← 위험도는 그대로 유지
│ │ 화물이 좌측으로 과도하게 기울었습니다.    │ │
│ └───────────────────────────────────────────┘ │
│ ┌───────────────────────────────────────────┐ │
│ │ ⏱ 마지막 감지 이후 47초 경과 —            │ │  ← 신선도만 별도 표시
│ │   최신 값이 아닐 수 있습니다.             │ │
│ └───────────────────────────────────────────┘ │
```

| 항목 | 내용 |
|---|---|
| **문구** | "⏱ 마지막 감지 이후 N초 경과 — 최신 값이 아닐 수 있습니다." |
| **데이터 출처** | `detectedAt` vs 현재 시각 (**임계값은 데이터 계약 대기**) |
| **다음 전이** | → 새 이벤트 수신 시 해제 |

> **stale은 위험도를 바꾸지 않는다.** WARNING이 오래됐다고 DANGER로 승격하거나 UNKNOWN으로 강등하지
> 않는다 — 두 축을 분리 표시한다. 오버레이도 유지하되 "⏱ 지연" 뱃지를 덧붙인다
> (Variant: `stale-warning` / `stale-danger`).

---

## 11. STOP 미연결 상태 (상태 8)

### 권장안 (prompt65 §7 채택)

```
│ ┌──────────────────────────────────────────┐ │
│ │  ⊘  STOP 준비 중                         │ │  disabled · 회색 · 커서 not-allowed
│ └──────────────────────────────────────────┘ │
│    일반 정지 기능이 아직 연결되지 않았습니다.│  보조 설명 (10px, slate-400)
│ ┌──────────────────────────────────────────┐ │
│ │  🛑  REAL-F01 비상정지                   │ │  활성 · 빨강 · 강조
│ └──────────────────────────────────────────┘ │
```

| 항목 | 내용 |
|---|---|
| **문구** | 라벨 "STOP 준비 중" / 보조 "일반 정지 기능이 아직 연결되지 않았습니다." |
| **활성** | 개별 비상정지만 |
| **비활성** | **STOP (`disabled`, `aria-disabled="true"`)** |
| **데이터 출처** | 정적 (API 미연결) |
| **사용자 행동** | 클릭 불가 |
| **다음 전이** | STOP API 연결 시 → `ready` Variant |

### 금지 (prompt65 §7)

| 금지 | 이유 |
|---|---|
| 눌러도 반응 없는 활성 버튼 | **현재 코드가 이 상태** — 운영자가 "눌렀으니 섰겠지"로 오해 |
| STOP → EMERGENCY_STOP API 연결 | 통상 정지와 비상 정지는 targetSystem·해제 요구가 다른 별개 명령 |
| STOP 즉시 화면 상태 변경 | 발행 성공이 정지 완료가 아니다 |

---

## 12. 개별 비상정지 상태 (상태 9~13)

### 12.1 확인 (상태 9)

```
┌────────────────────────────────────────┐
│ REAL-F01 차량에 비상정지 명령을        │
│ 전송하시겠습니까?                      │
│                    [취소]   [확인]     │
└────────────────────────────────────────┘
```

| 항목 | 내용 |
|---|---|
| **활성** | 취소, 확인 |
| **다음 전이** | 취소 → 원상복귀(**API 호출 없음**) · 확인 → 상태 10 |

### 12.2 요청 중 (상태 10)

```
│ [ ⬛ 명령 전송 중...                   ] │  disabled · aria-busy="true"
```

| 항목 | 내용 |
|---|---|
| **비활성** | 해당 차량 비상정지 (중복 클릭 차단) |
| **활성** | 전체 정지, **다른 차량** 비상정지 (차량별 독립 pending) |
| **다음 전이** | → 상태 11 / 12 |

### 12.3 발행 성공 (상태 11)

```
│ ✓ REAL-F01 비상정지 명령 전송 완료                                    [✕] │ 초록
│   commandId a1b2c3… · 실제 정지 여부는 차량 상태 이벤트로 확인됩니다.     │
```

| 항목 | 내용 |
|---|---|
| **데이터 출처** | 응답 본문 **`data.status === "PUBLISHED"`** (HTTP 201 아님) |
| **다음 전이** | → status 이벤트 도착 시 상태 13 |

> **PUBLISHED는 MQTT 발행 성공일 뿐 실제 정지 완료가 아니다.** 문구에 반드시 명시한다.

### 12.4 발행 실패 (상태 12)

```
│ ✕ REAL-F01 비상정지 명령 발행 실패                                    [✕] │ 빨강
│   MQTT 브로커로 명령을 발행하지 못했습니다. 차량은 정지되지 않았습니다.   │
```

| 항목 | 내용 |
|---|---|
| **데이터 출처** | `data.status === "PUBLISH_FAILED"` (**HTTP는 201**) |
| **사용자 행동** | 재시도 또는 전체 정지 |
| **다음 전이** | → 재시도 시 상태 10 |

> 네트워크·HTTP 오류와 **발행 실패는 다른 사건**이다. 전자는 "요청 실패", 후자는 "요청은 갔으나
> 브로커 발행 실패" — 문구를 구분한다.

### 12.5 차량 ESTOP (상태 13)

```
│ ● 비상정지                          10:33:02  │  상태 배지 = ESTOP
│ ┌──────────────────────────────────────────┐ │
│ │  ⊘  STOP 준비 중                         │ │ disabled
│ └──────────────────────────────────────────┘ │
│ ┌──────────────────────────────────────────┐ │
│ │  ⬛  비상정지 상태                       │ │ disabled
│ └──────────────────────────────────────────┘ │
│  ⓘ 이 화면에서는 비상정지를 해제할 수      │  ★ RESET_ESTOP 미지원
│    없습니다. 해제는 별도 절차가 필요합니다.│
```

| 항목 | 내용 |
|---|---|
| **활성** | 전체 정지, 미니맵 선택 |
| **비활성** | STOP, 개별 비상정지(이미 ESTOP) |
| **데이터 출처** | **`/topic/vehicles/status` → `ESTOP`** (유일한 근거) |
| **다음 전이** | 화면에서 **없음** — 해제는 외부 절차 |

> ESTOP은 **status 이벤트로만 확정**한다. 발행 성공(PUBLISHED)으로 상태를 바꾸지 않는다.

---

## 13. 전체 비상정지 상태 (상태 14·15)

### 13.1 확인 (상태 14)

```
┌──────────────────────────────────────────────┐
│ 활성 차량 3대에 비상정지 명령을 전송합니다.  │
│ 계속하시겠습니까?        [취소]   [확인]     │
└──────────────────────────────────────────────┘
```

| 항목 | 내용 |
|---|---|
| **데이터 출처** | `vehicles.filter(v => v.active).length` |
| **다음 전이** | 취소 → 원복(API 없음) · 확인 → 전송 중 |

### 13.2 결과 4분기 (상태 15 포함)

```
[전체 성공] ✓ 전체 비상정지 명령 전송 완료 (3/3대)                     초록
            실제 정지 여부는 각 차량 상태 이벤트로 확인됩니다.

[일부 실패] ⚠ 2대 전송 성공, 1대 발행 실패                            주황 ★
            실패 차량: SIM-F03

[전체 실패] ✕ 전체 비상정지 명령 발행 실패 (3/3대)                     빨강
            실패 차량: REAL-F01, REAL-F02, SIM-F03

[대상 0대] ⚠ 대상 활성 차량이 없습니다.                                주황
```

| 항목 | 내용 |
|---|---|
| **데이터 출처** | `requestedCount` / `publishedCount` / `failedCount` / `results[]` |
| **활성** | (완료 후) 전체 정지 재활성 |
| **다음 전이** | → 각 차량 status 이벤트 → 개별 ESTOP 표시 |

> ★ **부분 실패를 성공으로 표시하지 않는다.** 이 화면에서 가장 중요한 표시 규칙이다.
> 대상 수 필드명은 `totalCount`가 아니라 **`requestedCount`**다.

---

## 14. 연결 끊김 상태 (상태 16·17)

### 14.1 STOMP 끊김 (상태 16)

```
│ ⚠ 실시간 연결이 끊겼습니다. 표시된 값은 마지막 수신 시점 기준입니다. [다시 시도] │
├─────────────────────────────────────────────┬───────────────────────────────────┤
│ │ 디지털 트윈 영상   [⚠ 실시간 연결 끊김] │ │ 차량 상세                         │
│ │          (영상 유지)                    │ │  ※ 마지막 값 유지 — 지우지 않음   │
│ └─────────────────────────────────────────┘ │  ⏱ 마지막 감지 이후 2분 경과      │
```

| 항목 | 내용 |
|---|---|
| **문구** | "실시간 연결이 끊겼습니다. 표시된 값은 마지막 수신 시점 기준입니다." |
| **활성** | **비상정지 전부**(REST는 WebSocket과 독립 경로) |
| **비활성** | STOP |
| **데이터 출처** | `useMonitoringSocket` → `connected===false` |
| **다음 전이** | 재연결 → dashboard REST 재조회(2번째 연결부터) |

> **마지막 값을 지우지 않는다** — 빈 화면이 더 위험하다. 대신 "언제 것인지"를 명시한다.

### 14.2 영상 스트림 끊김 (상태 17)

```
│ │ 디지털 트윈 영상   [⬤ 실시간 연결됨]  │  ← STOMP는 정상
│ │                    [✕ 영상 연결 실패]  │
│ │  ┌───────────────────────────────────┐ │
│ │  │  ⚠  영상 연결에 실패했습니다      │ │
│ │  │     스트림 서버에 연결할 수 없음  │ │
│ │  │        [ ⟳ 다시 연결 ]            │ │
│ │  └───────────────────────────────────┘ │
```

| 항목 | 내용 |
|---|---|
| **활성** | 다시 연결, **모든 관제 기능**(상태·위치·적재·비상정지는 영상과 무관) |
| **데이터 출처** | `streamStatus` (**현재 dev 셀렉트로만 변경 — Mock**) |
| **다음 전이** | → connecting → connected/error |

> **2축 분리가 핵심**: 영상이 끊겨도 관제는 살아 있고, STOMP가 끊겨도 영상은 나올 수 있다.
> 배지 2개를 따로 두는 이유다.

---

## 15. 여러 위험 차량 상태 (상태 18)

```
┌──────────────────────────────────────────────────────────────────────────────────────┐
│ ⚠ 위험 차량 3대                                                                       │
│  [🛑 SIM-F03 · LOAD_UNSTABLE]  [🛑 REAL-F02 · LOAD_OFFSET_EXCEEDED]                  │
│  [⚠ REAL-F01 · LOAD_TILT_EXCEEDED]                                                   │
└──────────────────────────────────────────────────────────────────────────────────────┘
        ↑ DANGER 먼저, 그 다음 WARNING · 각 항목 클릭 → 해당 차량 선택
```

**미니맵 동시 표현**

```
┌────────────────────────────────────┐
│  ▣랙A               ▣랙B           │
│         ⓘ◆F01                      │   ◆=선택 + 주황 링 + ! 배지
│    ●F02        ⓘ▣F03               │   ▣=SIM + 빨강 링 + ! 배지
│  ▣랙C               ▣랙D           │
└────────────────────────────────────┘
```

| 항목 | 내용 |
|---|---|
| **문구** | "위험 차량 N대" + 차량별 `{vehicleId} · {riskCode}` |
| **활성** | 요약 항목 전부 클릭 가능 |
| **데이터 출처** | `loadSafetyByVehicleId` 중 `riskLevel ∈ {WARNING, DANGER}` |
| **사용자 행동** | 항목 클릭 → 선택 전환 → 상세 확인 |
| **다음 전이** | 위험 0대 → **요약 영역 DOM 제거** |

**규칙**
- WARNING·DANGER만 포함 (CAUTION·UNKNOWN 제외)
- **DANGER 우선 정렬**
- 오버레이는 **선택 차량 1대만** — 3대를 겹치면 영상이 가려진다. 나머지는 요약 스트립·미니맵으로 인지
- 프론트가 새 위험도를 계산하지 않는다. `riskLevel`·`riskCode`만 읽는다

---

## 16. 영역별 명세

| 영역 | 표시 내용 | 데이터 출처 | 사용자 동작 | 상태 변화 | 현재 구현 여부 |
|---|---|---|---|---|---|
| **헤더** | 제목·부제 | 정적 | 없음 | 없음 | **구현 완료** |
| 헤더 · 영상상태 셀렉트 | 영상 4상태 선택 | 로컬 state | 선택 | `streamStatus` | **Mock** (dev 전용, 제거 대상) |
| **위험 차량 요약** | "위험 N대" + `{id}·{riskCode}` | `loadSafetyByVehicleId` 필터 | 항목 클릭 → 선택 | `selectedVehicleId` | **미구현** |
| **디지털 트윈 영역** | 영상 | `DigitalTwinVideoLayer` | 없음 | dim 처리 | **Placeholder** |
| 영역 · 전체화면 | ⛶ | `document.fullscreenElement` | 클릭 | 전체화면 토글 | **구현 완료** |
| 영역 · 선택차량 오버레이 | id·상태·작업·source | `selectedVehicle` | 없음 | 선택 변경 시 | **구현 완료** |
| **적재 경고 오버레이** | 아이콘·경고/위험·id·riskCode·메시지 | `/topic/vehicles/load-safety` | **없음(클릭 통과)** | WARNING/DANGER만 렌더 | **구현 완료** |
| 오버레이 · stale 뱃지 | "⏱ 지연" | `detectedAt` | 없음 | 임계 초과 시 | **미구현** |
| **선택 차량 상세** | name·id·source·비활성 | dashboard | 없음 | — | **구현 완료** |
| 상세 · 상태 | 점+한글라벨+시각 | `/topic/vehicles/status` | 없음 | status 이벤트 | **구현 완료** |
| 상세 · 위치 | X·Y·Heading·Speed·시각 | `/topic/vehicles/location` | 없음 | location 이벤트 | **구현 완료** |
| 상세 · 현재 작업 | taskId·작업·명령 상태 | dashboard | 없음 | — | **구현 완료** |
| **적재 안전 패널** | 5단계 배지+riskCode+메시지 | `/topic/vehicles/load-safety` | 없음 | 이벤트 | **구현 완료** (데이터 계약 대기) |
| 패널 · 측정 6종 | 포크·화물·Roll·Pitch·편향XY | 동일 | 없음 | 동일 | **구현 완료** |
| 패널 · 미수신 | "데이터 미수신" 문구 | `null` | 없음 | — | **구현 완료** |
| 패널 · stale | "N초 경과" | `detectedAt` | 없음 | 시간 경과 | **부분 구현** (임계값 **데이터 계약 대기**) |
| **STOP 버튼** | "STOP 준비 중" + 보조설명 | 정적 | **클릭 불가** | 없음 | **미연결** |
| **개별 비상정지** | "{id} 비상정지"/"전송 중"/"비상정지 상태" | — | confirm→POST | pending→알림→disabled | **구현 완료** |
| 상세 · 해제 불가 안내 | "이 화면에서는 해제할 수 없습니다" | 정적 | 없음 | ESTOP 시 표시 | **미구현** |
| **미니맵** | 랙·통로 배경, "N/M 대" | 정적 + 렌더 가능 수 | 없음 | 위치 수신 | **구현 완료** |
| **미니맵 마커** | 상태색·방향·ID·REAL/SIM | `location`+`status` | **클릭 → 선택** | `selectedVehicleId` | **구현 완료** |
| 마커 · 위험 링/배지 | 링 + `!`/`?` + 지연 | `loadSafetyByVehicleId` | 없음 | riskLevel 변경 | **미구현** |
| **전체 비상정지** | 안내+대상 N대+버튼 | `vehicles.filter(active)` | confirm→POST | pending→알림 | **구현 완료** (⚠위치 상단) |
| 전체 · 자동정지 안내 | "자동으로 정지하지 않습니다" | 정적 | 없음 | 없음 | **미구현** |
| **명령 결과 알림** | tone+제목+상세 | 비상정지 응답 | [✕] 닫기 | `notice=null` | **부분 구현** (단일 슬롯·덮어쓰기) |
| **연결 상태 · STOMP** | 4상태 라벨+아이콘 | `useMonitoringSocket` | 없음 | 연결 변화 | **구현 완료** |
| **연결 상태 · 영상** | 4상태 라벨+아이콘 | `streamStatus` | 없음 | — | **Mock** |
| 명령 실행 결과 | `emergencyStopApplied` 등 | `/topic/vehicles/result` | 없음 | — | **미구현** (백엔드 계약 존재) |

---

## 16.10 미니맵 위험 차량 표시 설계

### 현재 마커 구조 (제약 조건)

`MiniMapVehicleMarker`는 **`loadSafety`를 prop으로 받지 않는다.** 위험 표시를 넣으려면 prop 추가가 필요하다.
그리고 다음 시각 언어가 **이미 점유돼 있다**:

| 시각 요소 | 현재 의미 |
|---|---|
| 마커 **모양** | 원형 = REAL / **사각 점선 = SIMULATION** |
| 마커 **색** | 차량 상태 10종 |
| **내부 pulse glow** | 선택됨 |
| 라벨 `ring-1 white` | 선택됨 |

> ⚠ **설계 충돌**: prompt65 §8은 UNKNOWN에 "회색 **점선** 링"을 제안하지만, **점선은 이미 SIMULATION
> 표식**이다. 점선 링을 쓰면 "SIM 차량"과 "판정 불가"가 뒤섞인다.
> → **UNKNOWN은 점선 대신 `?` 배지 + 회색 실선 링**으로 설계한다.

### 최종 마커 설계

```
      ┌─ 위험 배지 (우상단, 8px)
      │
   ╭──▼──╮
   │ ⓘ ! │  ← 외곽 위험 링 (2px, 마커보다 큰 반지름)
   │ ╭─╮ │
   │ │●│ │  ← 마커 본체 (상태색, REAL 원형 / SIM 사각점선) — 변경 없음
   │ ╰─╯ │  ← 선택 시 내부 pulse glow — 변경 없음
   ╰─────╯
     F01     ← ID 라벨
```

**두 축을 반지름으로 분리한다** — 선택은 **내부** glow, 위험은 **외곽** 링. 겹쳐도 둘 다 식별된다.

| riskLevel | 외곽 링 | 배지 | 접근성 라벨 |
|---|---|---|---|
| NORMAL | 없음 | 없음 | `정상` |
| CAUTION | 호박 1.5px 실선 | 없음 | `주의` |
| WARNING | 주황 2px 실선 | `!` 주황 | `경고` |
| DANGER | 빨강 2px 실선 (저강도 펄스) | `!` 빨강 | `위험` |
| UNKNOWN | 회색 1.5px **실선** | `?` 회색 | `판정 불가` |
| 데이터 미수신 | 없음 | `–` 흐린 회색 | `적재 데이터 없음` |
| stale | **위험 링 그대로 유지** + `⏱` | 기존 배지 유지 | `… (지연)` |

**규칙**
- 색상만 바꾸지 않는다 — 링 + **배지 문자** + `aria-label`을 함께 변경
- 프론트가 roll/pitch로 위험도를 계산하지 않는다. `riskLevel`을 그대로 읽는다
- 마커 클릭 = 선택 (팝업 없음, 기존 동작 유지)
- `aria-label` 최종형: `"F01 · 실제 · 이동 중 · 적재 경고 (지연) (선택됨)"`

> **미수신 표시 결정 필요**: 발행 주체가 아직 없어 **초기에는 전 차량이 미수신**이다. 모든 마커에
> `–` 배지가 붙으면 지도가 지저분해진다. → **옵션 A**: 미수신은 배지 없음(패널에서만 안내),
> **옵션 B**: 흐린 `–` 배지. 데이터 유입 시작 전까지는 **A 권장**. §18 P1에 결정 항목으로 둔다.

---

## 17. Figma 이관 명세

### 17.1 브레이크포인트 3종

#### 1920×1080 (기준)

```
Padding 12 · Gap 12
Header              56
[RiskSummary]       44   조건부
[Notice]            48   조건부
Main                flex-1   grid-cols-[2fr_1fr]
GlobalEStop         56
```

| 영역 | 비율 | 실측 |
|---|---|---|
| 좌측 영상 | 2fr | ~1264 |
| 우측 컬럼 | 1fr | ~632 |
| 우측 상단(상세) | 1.15fr | ~500 |
| 우측 하단(미니맵) | 1fr | ~435 |

#### 1440×900 (축소)

| 영역 | 비율 | 실측 |
|---|---|---|
| 좌측 영상 | 1.8fr | ~900 |
| 우측 컬럼 | 1fr | ~500 |

- 적재 측정값 2열 유지
- 위험 요약 스트립: 2줄까지 허용 후 "외 N대"

#### 1366×768 (최소)

| 영역 | 비율 | 실측 |
|---|---|---|
| 좌측 영상 | 1.6fr | ~810 |
| 우측 컬럼 | 1fr | ~506 |

- **적재 측정값 2열 → 3×2 유지하되 폰트 11px**
- 위험 요약: **1줄 + "외 N대"** 로 축약
- 하단 바 보조 문구는 1줄로 잘라 표시
- 우측 패널 **최소 너비 380px** — 이하에서는 측정값 1열로 전환

### 17.2 고정 수치

| 항목 | 값 |
|---|---|
| 우측 패널 최소 너비 | **380px** |
| 전체 비상정지 바 높이 | **56px** (보조 문구 포함 시 64px) |
| 헤더 높이 | 56px |
| 위험 요약 높이 | 44px (1줄) / 68px (2줄) |
| 패널 내부 여백 | **12px** |
| 패널 간 gap | 12px |
| 패널 내 요소 gap | 8~12px |
| 패널 radius | 8px |
| 배지·필드 radius | 6px |
| 오버레이 하단 여백 | 12px |
| 오버레이 최대 너비 | 672px (max-w-2xl) |

### 17.3 컴포넌트 계층

```
Page  [Auto V, Gap 12, Pad 12]
├─ Header                       [Auto H, Space Between, H56]
├─ RiskSummaryStrip ★           [Auto H, Wrap, Gap 8]        조건부
│  └─ RiskChip[]                [Auto H, Gap 6, Radius 6]
├─ NoticeStack                  [Auto V, Gap 8]              조건부
│  └─ CommandNotice[]
├─ Main                         [Grid 2fr 1fr, Gap 12, Fill]
│  ├─ VideoPanel                [Stack, Radius 8, Border]
│  │  ├─ VideoLayer             (fill)
│  │  ├─ TopLeftLabel           (abs 12,12)
│  │  ├─ TopRightControls       (abs r12,t12) [Auto H, Gap 8]
│  │  │  ├─ RealtimeBadge
│  │  │  ├─ StreamBadge
│  │  │  └─ FullscreenButton
│  │  ├─ SelectedVehicleCard    (abs r12,t44) [W208]
│  │  ├─ LoadSafetyOverlay ★    (abs b12, center) 조건부
│  │  └─ StreamStateOverlay     (abs fill, z30) 조건부
│  └─ RightColumn               [Grid rows 1.15fr 1fr, Gap 12, MinW 380]
│     ├─ VehicleDetailPanel     [Auto V, Gap 12, Pad 12, Radius 8]
│     │  ├─ PanelHeader
│     │  ├─ VehicleIdentity
│     │  ├─ StatusRow
│     │  ├─ LocationGrid        [2 cols, Gap 8]
│     │  ├─ LoadSafetySection   [Auto V, Gap 8]
│     │  │  ├─ RiskBadge
│     │  │  ├─ StaleNotice      조건부
│     │  │  ├─ MeasurementGrid  [2×3, Gap 8]
│     │  │  └─ CargoFooter
│     │  ├─ CurrentTask
│     │  └─ ControlStack        [Auto V, Gap 8]
│     │     ├─ StopButton       (unavailable)
│     │     ├─ StopHelperText ★
│     │     ├─ EStopButton
│     │     └─ ResetNotice ★    조건부(ESTOP)
│     └─ MiniMapPanel
│        └─ VehicleMarker[]
└─ GlobalEStopBar               [Auto H, Space Between, H56]
   ├─ InfoText + AutoStopNotice ★
   ├─ TargetCountChip
   └─ EStopAllButton
```
★ = 신규 컴포넌트

### 17.4 Variant 목록

```
VehicleMarker
  status    = unknown | idle | active | moving | lifting | loading
            | unloading | estop | error | offline
  risk      = none | caution | warning | danger | unknown | nodata
  selected  = true | false
  stale     = true | false
  source    = real | sim
  → 대표 조합: selected / normal / caution / warning / danger / unknown / stale / offline

LoadSafetyOverlay
  = hidden | warning | danger | stale-warning | stale-danger

StopButton
  = unavailable | ready | pending | disabled
    (현재 사용: unavailable 만)

EStopButton
  = ready | pending | disabled-estop

RiskBadge
  = normal | caution | warning | danger | unknown

LoadSafetySection
  = data | empty | stale

CommandNotice
  = success | warning | error

ConnectionBadge
  type  = realtime | stream
  state = connecting | connected | disconnected | error

RiskSummaryStrip
  = hidden | single | multiple | overflow

GlobalEStopBar
  = ready | pending | no-target
```

### 17.5 상태별 컴포넌트 이름 (화면 상태 ↔ Variant)

| # | 화면 상태 | 주요 Variant |
|---|---|---|
| 1 | 차량 미선택 | `VehicleDetailPanel/empty` |
| 2 | 정상 | `RiskBadge/normal` + `LoadSafetyOverlay/hidden` |
| 3 | CAUTION | `RiskBadge/caution` + `VehicleMarker/caution` |
| 4 | WARNING | `RiskBadge/warning` + `LoadSafetyOverlay/warning` |
| 5 | DANGER | `RiskBadge/danger` + `LoadSafetyOverlay/danger` |
| 6 | 미수신 | `LoadSafetySection/empty` + `VehicleMarker/nodata` |
| 7 | stale | `LoadSafetySection/stale` + `LoadSafetyOverlay/stale-*` |
| 8 | STOP 미연결 | `StopButton/unavailable` |
| 9 | 개별 확인 | (네이티브 confirm) |
| 10 | 개별 요청 중 | `EStopButton/pending` |
| 11 | 발행 성공 | `CommandNotice/success` |
| 12 | 발행 실패 | `CommandNotice/error` |
| 13 | ESTOP | `EStopButton/disabled-estop` + `ResetNotice` |
| 14 | 전체 확인 | `GlobalEStopBar/pending` |
| 15 | 일부 실패 | `CommandNotice/warning` |
| 16 | STOMP 끊김 | `ConnectionBadge/realtime-disconnected` |
| 17 | 영상 끊김 | `ConnectionBadge/stream-error` |
| 18 | 다중 위험 | `RiskSummaryStrip/multiple` |

### 17.6 디자인 토큰

| 토큰 | 값 |
|---|---|
| `bg/app` | `#020617` slate-950 |
| `bg/panel` | `#0b1220` |
| `bg/subtle` | `rgba(255,255,255,.05)` |
| `border/default` | `#334155` slate-700 |

**위험 단계** (배경 `950/40~50`, 테두리 `500/30~50`)

| 단계 | 주색 | 텍스트 |
|---|---|---|
| NORMAL | emerald-500 `#10b981` | emerald-100 |
| CAUTION | amber-500 `#f59e0b` | amber-100 |
| WARNING | orange-500 `#f97316` | orange-100 |
| DANGER | red-500 `#ef4444` | red-100 |
| UNKNOWN | slate-500 `#64748b` | slate-200 |

**차량 상태 10종** — `components/monitoring/vehicle-status.ts` 값 그대로

| 상태 | base | 상태 | base |
|---|---|---|---|
| MOVING | `#10b981` | UNLOADING | `#fb923c` |
| IDLE | `#3b82f6` | ESTOP | `#ef4444` |
| ACTIVE | `#eab308` | ERROR | `#dc2626` |
| LIFTING | `#a855f7` | OFFLINE | `#64748b` |
| LOADING | `#f59e0b` | UNKNOWN | `#64748b` |

**타이포** (Geist Sans / Geist Mono)

| 역할 | px | weight |
|---|---|---|
| 페이지 제목 | 16 | 600 |
| 패널 제목 | 12 | 600 |
| 본문 | 12 | 400 |
| 라벨(uppercase) | 10 | 500 |
| 수치(mono) | 12 | 400 |
| 오버레이 제목 | 14 | 700 |
| 버튼 | 14 | 600 |
| 보조 설명 | 10 | 400 |

### 17.7 접근성 주의사항

| # | 항목 |
|---|---|
| 1 | **색상 단독 사용 금지** — 모든 상태는 아이콘 + 텍스트 라벨 동반 |
| 2 | 오버레이 `role="alert"` `aria-live="assertive"`, 정상 알림은 `role="status"` `aria-live="polite"` |
| 3 | 오버레이 등장 시 **포커스를 옮기지 않는다**(작업 방해) — 현재 구현이 이미 이 방식 |
| 4 | `pointer-events-none` 으로 오버레이가 조작을 막지 않게 |
| 5 | 비활성 버튼은 `disabled` + `aria-disabled="true"` + 이유 텍스트 |
| 6 | 진행 중 버튼은 `aria-busy="true"` |
| 7 | 마커는 `<button>` + `aria-pressed` + 상태·위험 포함 `aria-label` (현재 구조 유지) |
| 8 | 미니맵 마커 **키보드 순회 가능해야 함** — 현재 `<button>`이라 Tab 가능, **포커스 링 확인 필요** |
| 9 | `prefers-reduced-motion` 시 pulse·트랜지션 정지 (**현재 선택 glow의 `animate-pulse`가 미대응**) |
| 10 | 수치는 `tabular-nums` — 실시간 갱신 시 폭 흔들림 방지 |
| 11 | **대비비 미검증**: 주황 계열 `orange-100 on orange-950` WCAG AA 확인 필요 |
| 12 | 위험 요약 항목은 `<button>` — 클릭 가능함이 스크린리더에 전달돼야 함 |

---

## 18. 구현 우선순위

### P0 — 안전 오해를 막는 작업

| # | 작업 | 근거 |
|---|---|---|
| 1 | **STOP 버튼 disabled + "STOP 준비 중" + 보조설명** | 현재 눌러도 무반응·무피드백 → "눌렀으니 섰다"는 치명적 오해 |
| 2 | **ESTOP 시 RESET_ESTOP 미지원 안내** | 화면에서 해제 불가한데 안내가 없다. 되돌릴 수 없는 조작 |
| 3 | **"자동 정지 없음" 상시 안내** | DANGER를 봐도 시스템은 멈추지 않는다 — 사전에 알아야 함 |
| 4 | **미수신 ≠ 정상 표시 검증** | 이미 구현됨. 회귀 방지를 위해 문구 고정 |

### P1 — 위험 차량 인지 누락을 막는 작업

| # | 작업 | 근거 |
|---|---|---|
| 5 | **미니맵 위험 링 + 배지** | 선택하지 않은 차량이 DANGER여도 **화면 어디에도 표시가 없다** — 현재 최대 인지 공백 |
| 6 | **위험 차량 요약 스트립** | 다중 위험 시 하나씩 클릭해야만 파악 가능 |
| 7 | **알림 스택(최대 3)** | 새 알림이 이전 것을 덮어씀 — 개별 후 전체 정지 시 앞 결과 소실 |
| 8 | 미수신 마커 표시 방식 결정 | §16.10 옵션 A/B |
| 9 | 적재 stale 임계값 확정 | 15초는 잠정값 — **센서 주기 계약 필요** |

### P2 — 레이아웃 및 정보 구조 개선

| # | 작업 | 근거 |
|---|---|---|
| 10 | **전체 비상정지 바 하단 이동** | 확정 구조와의 유일한 차이 |
| 11 | dev 영상상태 셀렉트 제거 | 운영 화면에 개발 컨트롤 노출 |
| 12 | 영상 placeholder 명시적 처리 | 샘플 스틸이 실영상으로 오해될 수 있음 |
| 13 | 명령 결과(`/topic/vehicles/result`) 구독 | "발행됨"과 "실제 멈춤"을 구분. **백엔드 계약 이미 존재** |
| 14 | 1366 대응 (측정값 1열 전환) | 노트북에서 2열 깨짐 |

### P3 — 시각적 품질 및 접근성

| # | 작업 |
|---|---|
| 15 | 수치 `tabular-nums` |
| 16 | `prefers-reduced-motion` 대응 (선택 glow·DANGER 펄스) |
| 17 | 상태 전환 트랜지션 150ms |
| 18 | 패널 헤더 한영 병기 규칙 통일 |
| 19 | 주황 계열 대비비 WCAG AA 검증 |
| 20 | `window.confirm` → 프로젝트 스타일 모달 |
| 21 | 기울기 수평계 시각화 (임계선은 **그리지 않음** — 계약 없음) |

### 별도 평가 항목 (prompt65 §11 지정)

| 항목 | 등급 | 판단 |
|---|---|---|
| STOP 버튼 미연결 | **P0-1** | 무반응 활성 버튼이 가장 위험한 오해 |
| 전체 비상정지 위치 | **P2-10** | 안전 기능이지만 **동작은 정상** — 위치만 다름 |
| 미니맵 위험 링 | **P1-5** | 인지 공백. 안전 오해는 아니나 누락 위험 |
| 위험 차량 요약 | **P1-6** | 다중 위험 대응 |
| RESET_ESTOP 안내 | **P0-2** | 비가역 조작 경고 |
| dev 셀렉트 제거 | **P2-11** | 혼란 요인이나 안전 직결 아님 |
| 영상 placeholder | **P2-12** | 실영상 오해 방지 |
| 알림 스택 | **P1-7** | 실패 알림 소실 = 안전 정보 소실 |
| `tabular-nums` | **P3-15** | 순수 시각 품질 |
| `prefers-reduced-motion` | **P3-16** | 접근성 |

---

## 19. 구현 시 주의사항

### 19.1 안전 표시 불변 규칙

| # | 규칙 |
|---|---|
| 1 | **프론트는 위험도를 계산하지 않는다.** roll/pitch를 임계값과 비교하는 코드를 추가하지 않는다 |
| 2 | 받은 `riskLevel`을 그대로 표시한다. 모르는 값은 `NORMAL`이 아니라 **`UNKNOWN`** |
| 3 | **색상만으로 구분하지 않는다.** 아이콘 + 텍스트 필수 |
| 4 | WARNING·DANGER만 오버레이. CAUTION·UNKNOWN은 오버레이 없음 |
| 5 | **DANGER여도 ESTOP API를 자동 호출하지 않는다** |
| 6 | **실제 ESTOP은 `/topic/vehicles/status` 이벤트로만 확정**한다 |
| 7 | **PUBLISHED는 발행 성공일 뿐 정지 완료가 아니다** — 문구에 명시 |
| 8 | STOP과 EMERGENCY STOP은 별개 기능. 같은 API에 연결하지 않는다 |
| 9 | RESET_ESTOP 미지원을 ESTOP 상태에서 안내한다 |
| 10 | 미연결 버튼을 활성처럼 보이게 하지 않는다 |
| 11 | **데이터 미수신과 정상을 혼동하지 않게 한다** |
| 12 | **stale과 위험도를 별도 축으로 표시**한다. stale이 riskLevel을 바꾸지 않는다 |
| 13 | 부분 실패를 성공으로 표시하지 않는다 |

### 19.2 데이터 계약 대기 항목 (임의 확정 금지)

| 항목 | 현재 | 필요 |
|---|---|---|
| stale 임계값 | 15초(잠정) | **센서 발행 주기** |
| riskLevel 값 집합 | 5종(백엔드 채택) | 비전 팀 확정 |
| riskCode 목록 | 예시만 | 명명 규칙·전체 목록 |
| 기울기 임계선 | **그리지 않음** | 임계값 계약 |
| 측정 단위 | m·degree(추정) | 확정 |
| 영상 스트림 | placeholder | 프로토콜 확정 |

> 위 값들을 시안에서 **확정된 것처럼 표기하지 않는다.** 특히 기울기 시각화에 임계선을 그리면
> 없는 계약을 만들어내는 셈이 된다.

### 19.3 구현 시 흔한 실수

| # | 실수 | 결과 |
|---|---|---|
| 1 | `response.ok`로 비상정지 성공 판정 | PUBLISH_FAILED도 **HTTP 201** → 실패를 성공으로 표시 |
| 2 | `totalCount` 필드 사용 | 실제 필드는 **`requestedCount`** |
| 3 | 발행 성공 시 status를 ESTOP으로 변경 | optimistic update 금지 — 이벤트만이 근거 |
| 4 | 차량별 토픽 추가 구독 | 공통 토픽과 **중복 수신** |
| 5 | 안전명령 이벤트를 `RealtimeEvent` 봉투로 파싱 | `/topic/vehicles/commands` 계열은 **평면 payload** |
| 6 | 미측정 값을 0으로 표시 | "기울기 0"과 "측정 안 됨"이 섞임 → **`—`** |
| 7 | UNKNOWN을 오버레이 대상에 포함 | 판정 불가를 위험으로 승격 → 오탐 폭증 |
| 8 | 위험 마커에 점선 링 사용 | **점선은 이미 SIMULATION 표식** (§16.10) |
| 9 | 다중 위험 시 오버레이 여러 개 | 영상이 가려짐 → 선택 차량 1대만 |
| 10 | 연결 끊김 시 값 초기화 | 빈 화면이 더 위험 → 마지막 값 + 시각 표시 |

---

## 부록. 근거 파일

| 영역 | 파일 |
|---|---|
| 레이아웃 | `app/page.tsx` |
| 영상 | `MainRealtimeMonitoringView.tsx`, `DigitalTwinVideoLayer.tsx` |
| 배지 | `RealtimeConnectionBadge.tsx`, `ConnectionStatusBadge.tsx`, `FullscreenButton.tsx` |
| 차량 상세 | `VehicleDetailPanel.tsx`, `SelectedVehicleOverlay.tsx` |
| 적재 안전 | `LoadSafetyPanel.tsx`, `LoadSafetyOverlay.tsx`, `types/loadSafety.ts`, `lib/loadSafety.ts`, `lib/api/loadSafetyApi.ts` |
| 미니맵 | `MiniMap.tsx`, `MiniMapVehicleMarker.tsx`, `lib/coordinate.ts`, `lib/vehicleStatus.ts` |
| 비상정지 | `GlobalEmergencyStopBar.tsx`, `CommandNotice.tsx`, `lib/api/commandApi.ts`, `types/command.ts` |
| 상태 토큰 | `components/monitoring/vehicle-status.ts` |
| 실시간 | `hooks/useMonitoringSocket.ts`, `hooks/useMonitoringDashboard.ts`, `types/websocket.ts` |
| 공통 | `lib/api/httpClient.ts`, `types/api.ts`, `types/monitoring.ts`, `lib/format.ts` |

> `types/vehicle.ts`는 **존재하지 않는다** — `types/monitoring.ts`가 그 역할을 한다.
