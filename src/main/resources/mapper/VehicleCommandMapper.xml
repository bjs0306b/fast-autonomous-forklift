<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fast.backend.command.mapper.VehicleCommandMapper">
    <resultMap id="result" type="com.fast.backend.command.domain.VehicleCommand">
        <id property="commandId" column="command_id"/>
        <result property="taskId" column="task_id"/>
        <result property="vehicleId" column="vehicle_id"/>
        <result property="command" column="command"/>
        <result property="targetSystem" column="target_system"/>
        <result property="status" column="status"/>
        <result property="resultMessage" column="result_message"/>
        <result property="completedAt" column="completed_at"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>
    <sql id="columns">
        command_id, task_id, vehicle_id, command, target_system, status,
        result_message, completed_at, created_at
    </sql>
    <insert id="insert" parameterType="com.fast.backend.command.domain.VehicleCommand">
        INSERT INTO vehicle_command
            (command_id, task_id, vehicle_id, command, target_system, status, created_at)
        VALUES
            (#{commandId}, #{taskId}, #{vehicleId}, #{command}, #{targetSystem}, #{status}, #{createdAt})
    </insert>
    <update id="update" parameterType="com.fast.backend.command.domain.VehicleCommand">
        UPDATE vehicle_command
        SET status=#{status}, result_message=#{resultMessage}, completed_at=#{completedAt}
        WHERE command_id=#{commandId}
    </update>
    <select id="existsByCommandId" resultType="boolean">
        SELECT COUNT(*) &gt; 0 FROM vehicle_command WHERE command_id=#{commandId}
    </select>
    <select id="findByCommandId" resultMap="result">
        SELECT <include refid="columns"/> FROM vehicle_command WHERE command_id=#{commandId}
    </select>
    <select id="findRecentByVehicleId" resultMap="result">
        SELECT <include refid="columns"/> FROM vehicle_command
        WHERE vehicle_id=#{vehicleId}
        ORDER BY created_at DESC, command_id DESC
        LIMIT #{limit}
    </select>
</mapper>
