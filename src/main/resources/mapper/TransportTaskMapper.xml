<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--
    FR-202 최종 스키마(prompt85). pallet_id / cargo_orientation / updated_at 이 없고
    destination_slot_code(VARCHAR) 와 measurement_id(NOT NULL) 가 있다.
    배정·상태 전이는 조건부 UPDATE 로 경쟁을 DB 에서 판정한다.
-->
<mapper namespace="com.fast.backend.transport.mapper.TransportTaskMapper">

    <resultMap id="transportTaskResultMap" type="com.fast.backend.transport.domain.TransportTask">
        <id property="id" column="id"/>
        <result property="taskCode" column="task_code"/>
        <result property="cargoId" column="cargo_id"/>
        <result property="measurementId" column="measurement_id"/>
        <result property="vehicleId" column="vehicle_id"/>
        <result property="sourceX" column="source_x"/>
        <result property="sourceY" column="source_y"/>
        <result property="sourceHeading" column="source_heading"/>
        <result property="destinationSlotCode" column="destination_slot_code"/>
        <result property="destinationX" column="destination_x"/>
        <result property="destinationY" column="destination_y"/>
        <result property="destinationHeading" column="destination_heading"/>
        <result property="forkHeight" column="fork_height"/>
        <result property="status" column="status"/>
        <result property="assignedAt" column="assigned_at"/>
        <result property="startedAt" column="started_at"/>
        <result property="pickedUpAt" column="picked_up_at"/>
        <result property="completedAt" column="completed_at"/>
        <result property="failedAt" column="failed_at"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <sql id="allColumns">
        id, task_code, cargo_id, measurement_id, vehicle_id,
        source_x, source_y, source_heading,
        destination_slot_code, destination_x, destination_y, destination_heading,
        fork_height, status, assigned_at, started_at, picked_up_at, completed_at, failed_at, created_at
    </sql>

    <!-- 차량이 잡혀 진행 중인 상태. 완료/실패/취소와 미배정(PENDING)은 제외한다. -->
    <sql id="activeStatuses">
        ('ASSIGNED', 'MOVING_TO_PICKUP', 'PICKING_UP', 'TRANSPORTING', 'PLACING')
    </sql>

    <!-- 아직 종결되지 않은 상태(미배정 포함). -->
    <sql id="openStatuses">
        ('PENDING', 'ASSIGNED', 'MOVING_TO_PICKUP', 'PICKING_UP', 'TRANSPORTING', 'PLACING')
    </sql>

    <insert id="insert" parameterType="com.fast.backend.transport.domain.TransportTask"
            useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO transport_task
            (task_code, cargo_id, measurement_id, vehicle_id, source_x, source_y, source_heading,
             destination_slot_code, destination_x, destination_y, destination_heading,
             fork_height, status, created_at)
        VALUES
            (#{taskCode}, #{cargoId}, #{measurementId}, #{vehicleId}, #{sourceX}, #{sourceY}, #{sourceHeading},
             #{destinationSlotCode}, #{destinationX}, #{destinationY}, #{destinationHeading},
             #{forkHeight}, #{status}, #{createdAt})
    </insert>

    <select id="findById" resultMap="transportTaskResultMap">
        SELECT <include refid="allColumns"/> FROM transport_task WHERE id = #{id}
    </select>

    <select id="findByTaskCode" resultMap="transportTaskResultMap">
        SELECT <include refid="allColumns"/> FROM transport_task WHERE task_code = #{taskCode}
    </select>

    <select id="findAll" resultMap="transportTaskResultMap">
        SELECT <include refid="allColumns"/>
        FROM transport_task
        <where>
            <if test="status != null">AND status = #{status}</if>
            <if test="vehicleId != null">AND vehicle_id = #{vehicleId}</if>
        </where>
        ORDER BY created_at DESC, id DESC
        LIMIT #{size} OFFSET #{offset}
    </select>

    <select id="countAll" resultType="long">
        SELECT COUNT(*)
        FROM transport_task
        <where>
            <if test="status != null">AND status = #{status}</if>
            <if test="vehicleId != null">AND vehicle_id = #{vehicleId}</if>
        </where>
    </select>

    <select id="findActiveTasksWithVehicle" resultMap="transportTaskResultMap">
        SELECT <include refid="allColumns"/>
        FROM transport_task
        WHERE vehicle_id IS NOT NULL
          AND status IN <include refid="activeStatuses"/>
        ORDER BY created_at
    </select>

    <update id="updateAssignment">
        UPDATE transport_task
        SET vehicle_id = #{vehicleId},
            status = 'ASSIGNED',
            assigned_at = #{assignedAt}
        WHERE task_code = #{taskCode}
          AND status = 'PENDING'
          AND vehicle_id IS NULL
    </update>

    <update id="updateStatus">
        UPDATE transport_task
        SET status = #{status}
            <if test="startedAt != null">, started_at = #{startedAt}</if>
            <if test="pickedUpAt != null">, picked_up_at = #{pickedUpAt}</if>
            <if test="completedAt != null">, completed_at = #{completedAt}</if>
            <if test="failedAt != null">, failed_at = #{failedAt}</if>
        WHERE task_code = #{taskCode}
    </update>

    <select id="existsActiveTaskByVehicleId" resultType="boolean">
        SELECT COUNT(*) > 0 FROM transport_task
        WHERE vehicle_id = #{vehicleId} AND status IN <include refid="activeStatuses"/>
    </select>

    <select id="existsOpenTaskByCargoId" resultType="boolean">
        SELECT COUNT(*) > 0 FROM transport_task
        WHERE cargo_id = #{cargoId} AND status IN <include refid="openStatuses"/>
    </select>

    <select id="existsActiveTaskBySlotCode" resultType="boolean">
        SELECT COUNT(*) > 0 FROM transport_task
        WHERE destination_slot_code = #{slotCode} AND status IN <include refid="activeStatuses"/>
    </select>

</mapper>
