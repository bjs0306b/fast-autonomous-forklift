<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fast.backend.vehicle.mapper.VehicleMapper">

    <resultMap id="vehicleResultMap" type="com.fast.backend.vehicle.domain.Vehicle">
        <!--
            FR-202 최종 스키마: vehicle 은 vehicle_id(PK)/name/active/created_at/updated_at 뿐이다.
            대리키 id, source(REAL/SIMULATION), vehicle_type 은 DB·도메인·API 에서 모두 제거됐다(prompt90).
            vehicle_id 가 자연키 PK 이므로 useGeneratedKeys 를 쓰지 않는다.
        -->
        <id property="vehicleId" column="vehicle_id"/>
        <result property="name" column="name"/>
        <result property="active" column="active"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <insert id="insert" parameterType="com.fast.backend.vehicle.domain.Vehicle">
        INSERT INTO vehicle (vehicle_id, name, active, created_at, updated_at)
        VALUES (#{vehicleId}, #{name}, #{active}, #{createdAt}, #{updatedAt})
    </insert>

    <select id="findByVehicleId" resultMap="vehicleResultMap">
        SELECT vehicle_id, name, active, created_at, updated_at
        FROM vehicle
        WHERE vehicle_id = #{vehicleId}
    </select>

    <select id="existsByVehicleId" resultType="boolean">
        SELECT COUNT(*) > 0
        FROM vehicle
        WHERE vehicle_id = #{vehicleId}
    </select>

    <update id="updateActive">
        UPDATE vehicle
        SET active = #{active},
            updated_at = #{updatedAt}
        WHERE vehicle_id = #{vehicleId}
    </update>

    <select id="findAllActive" resultMap="vehicleResultMap">
        SELECT vehicle_id, name, active, created_at, updated_at
        FROM vehicle
        WHERE active = true
        ORDER BY vehicle_id
    </select>

    <select id="findAllActiveVehicleIds" resultType="string">
        SELECT vehicle_id
        FROM vehicle
        WHERE active = true
        ORDER BY vehicle_id
    </select>

</mapper>
