-- =============================================================================
-- 랙 슬롯 좌표 시드 (A001~A012, B001~B012 = 24칸) — MySQL 로컬 수동 실행 전용
-- =============================================================================
-- 🚫 이 파일은 spring.sql.init 대상이 **아니다**. 어떤 프로필의 data-locations 에도 넣지 말 것
--    (2026-08-07 확인: local/mqttcheck/test 어디에서도 참조하지 않는다).
--    아래 `USE fast_backend;` 때문에 H2 에서는 실행되지 않는다 — H2 는 이 문장을
--    "Schema \"FAST_BACKEND\" not found" 로 거부한다(실측). 자동 실행이 필요해지면
--    USE 를 먼저 지우고 data-local.sql 처럼 DB 지정을 실행하는 쪽에 맡겨야 한다.
--
-- 대상 DB 를 파일이 직접 고정한다 — DataGrip/Workbench 콘솔의 기본 스키마가 비어 있으면
-- ERROR 1046 (3D000): No database selected 가 나기 때문이다.
USE fast_backend;
-- =============================================================================
-- 출처: F팀 `backend-mqtt-guide` (2026-08-05) §6 "랙 슬롯"
--
--   | 랙 | 접근점 x | 도킹 x | 적재점 x | y (12칸)                                    |
--   | A  | 5.0      | 2.60   | 0.95     | 9.3 10.5 11.8 13.2 14.5 15.8 17.2 18.5      |
--   | B  | 14.5     | 11.95  | 10.20    | 19.7 21.2 22.5 23.8                         |
--   선반 높이(포크 목표) 1.325 · 진입 yaw 3.1416(서쪽)
--   ⚠️ 8/5 판에는 13.25 로 적혀 있었다. 8/9 개정판·rack_slots.csv·shelfHeight 예시가
--      모두 1.325 이므로 그쪽이 맞다(13.25 는 포크 최대치 1.5 를 8 배 넘는 값이다).
--
-- 기존 테스트 슬롯(SLOT-01/02)은 지우지 않는다. 필요하면 아래 5장 참고.
--
-- =============================================================================
-- 1. 단위 — 시뮬 좌표계다 (실물의 10 배). 백엔드는 변환하지 않는다
-- =============================================================================
-- F팀 backend-mqtt-guide §2: "실물 세트장은 그 1/10 인 2 × 3 m 이고, 변환은 실물 차량이
-- 자기 쪽에서 한다(보낼 때 ×10, 받을 때 ÷10). 백엔드는 아무 변환도 하지 않는다."
--
--   destination_x / destination_y   시뮬 좌표 (0~20 × 0~30)  ← 그대로 task 로 나감
--   fork_height                     시뮬 단위 (1.325)         ← 그대로 task.dropoff 로 나감
--   usable_height / usable_width    실물 m                    ← 카메라가 잰 화물과 비교
--
-- 즉 DB 에 시뮬 값을 넣는 것은 맞고, 높이 판정 컬럼만 실물 m 이다. 같은 문서가
-- "cargo 의 height 만 실물 미터"라고 명시하기 때문이다.
--
-- ⚠️ 2026-08-07 에 "시뮬 좌표를 m 로 본다"고 정리했던 것은 잘못이다. rack_slots.csv 에
--    approach_x 5.0 ↔ real_approach_x 0.5 처럼 두 계열이 나란히 있어 10 배 관계가 명확하다.
--    백엔드가 할 일(변환 안 함)은 같지만, 값 자체를 실물로 착각하면 안 된다.
--
-- =============================================================================
-- 2. usable_height 2.00 m — F팀 문서에 없어 팀이 정한 값
-- =============================================================================
-- 문서가 준 것은 좌표와 포크 목표 높이뿐이고, "한 칸에 얼마나 높은 화물이 들어가는가"는
-- 없다. 그런데 컬럼이 NOT NULL 이고 CHECK(usable_height > 0) 이라 비워 둘 수 없다.
--
-- 2026-08-07 결정: **2.00 m** (직전 값 1.00 m 는 너무 낮아 대부분의 화물이 거부됐다).
-- 이 값으로 통과하는 화물 높이는 다음과 같다.
--
--     usable_height 2.00 ≥ 화물높이 + 팔레트 0.12 + 여유 0.25
--     → 화물높이 ≤ 1.63 m 까지 적재 가능
--
-- 실측값이 아니라 합의값이다. 실측이 나오면 갱신할 것:
--     UPDATE storage_slot SET usable_height = <실측 m> WHERE slot_code = 'A001';
--
-- usable_width 는 NULL 로 둔다 — NULL 은 "폭 제약을 모른다"는 뜻이고 폭 검사를 건너뛴다.
-- 0 으로 채우면 모든 화물이 탈락하므로 절대 쓰지 말 것(PlacementService 주석 참고).
--
-- =============================================================================
-- 3. 깊이(depth)는 재지 않기로 했다 — 컬럼도 두지 않는다
-- =============================================================================
-- 스테이션 카메라가 정면 하나뿐이라 ai/src/station/pipeline.py 가 depth_cm 을 항상 null
-- 로 낸다(규격 v1.0 명시). 측정할 수 없는 값을 슬롯 쪽에만 넣어 두면 "검사하는 것처럼"
-- 보이므로 storage_slot 에도 깊이 컬럼을 두지 않는다.
--
-- 같은 이유로 화물 회전(가로↔세로 교환) 판정도 하지 않는다 — 두 축을 모두 알아야 성립한다.
--
-- =============================================================================
-- 4. destination_x 에 "접근점"을 넣는 이유
-- =============================================================================
-- F팀 문서: "접근점까지만 Nav2 로 가고, 그 뒤 도킹·적재·후진은 차량이 자체 수행한다.
--            백엔드는 접근점 도착을 확인한 뒤 place_rack 만 보내면 된다."
-- 따라서 도킹 x(2.60/11.95)·적재점 x(0.95/10.20)는 차량 몫이라 DB 에 넣지 않는다.
-- 넣어 두면 백엔드가 그리로 보내야 하는 것처럼 오해를 부른다.
--
-- destination_heading 은 degree 다(스키마 주석 기준). 문서의 yaw 3.1416 rad = 180.0 도.
-- =============================================================================

-- 5. 기존 테스트 슬롯을 지우고 싶다면 이 두 줄의 주석을 풀 것.
--    ⚠️ 예약/적재 중인 슬롯이 있으면 FK 때문에 실패한다. 상태부터 확인하라.
-- DELETE FROM storage_slot WHERE slot_code IN ('SLOT-01', 'SLOT-02')
--   AND status = 'EMPTY' AND reserved_task_id IS NULL AND stored_cargo_id IS NULL;

-- 6. 구 코드(A1~A12, B1~B12)로 이미 넣었다면 이 줄의 주석을 풀어 먼저 지울 것.
--    INSERT IGNORE 는 slot_code 가 PK 라 'A1' 과 'A001' 을 다른 칸으로 보고 둘 다 남긴다.
--    ⚠️ EMPTY 인 것만 지운다 — 예약/적재 중인 칸을 지우면 진행 중 작업이 깨진다.
-- DELETE FROM storage_slot
--  WHERE slot_code REGEXP '^[AB][0-9]{1,2}$' AND status = 'EMPTY'
--    AND reserved_task_id IS NULL AND stored_cargo_id IS NULL;

-- 슬롯 코드는 A001 형식으로 세 자리를 채운다 — slot_code 가 VARCHAR 라 정렬이 문자열
-- 기준이기 때문이다. 'A1','A2','A10' 은 A1 → A10 → A11 → A2 순으로 서지만,
-- 'A001','A002','A010' 은 번호순 그대로 선다. PlacementService 가 동점일 때
-- slotCode 로 최종 결정하므로 이 순서가 곧 적재 순서가 된다.

-- INSERT IGNORE: 이미 있는 슬롯은 건드리지 않는다(예약·적재 상태를 덮어쓰면 안 된다).
INSERT IGNORE INTO storage_slot
    (slot_code, usable_height, usable_width, fork_height,
     destination_x, destination_y, destination_heading, status) VALUES
-- ── 랙 A (왼쪽 벽) · 접근점 x = 5.0 ──────────────────────────────────────────
    ('A001', 2.00, NULL, 1.325,  5.0,  9.3, 180.0, 'EMPTY'),
    ('A002', 2.00, NULL, 1.325,  5.0, 10.5, 180.0, 'EMPTY'),
    ('A003', 2.00, NULL, 1.325,  5.0, 11.8, 180.0, 'EMPTY'),
    ('A004', 2.00, NULL, 1.325,  5.0, 13.2, 180.0, 'EMPTY'),
    ('A005', 2.00, NULL, 1.325,  5.0, 14.5, 180.0, 'EMPTY'),
    ('A006', 2.00, NULL, 1.325,  5.0, 15.8, 180.0, 'EMPTY'),
    ('A007', 2.00, NULL, 1.325,  5.0, 17.2, 180.0, 'EMPTY'),
    ('A008', 2.00, NULL, 1.325,  5.0, 18.5, 180.0, 'EMPTY'),
    ('A009', 2.00, NULL, 1.325,  5.0, 19.7, 180.0, 'EMPTY'),
    ('A010', 2.00, NULL, 1.325,  5.0, 21.2, 180.0, 'EMPTY'),
    ('A011', 2.00, NULL, 1.325,  5.0, 22.5, 180.0, 'EMPTY'),
    ('A012', 2.00, NULL, 1.325,  5.0, 23.8, 180.0, 'EMPTY'),
-- ── 랙 B (가운데) · 접근점 x = 14.5 ─────────────────────────────────────────
    ('B001', 2.00, NULL, 1.325, 14.5,  9.3, 180.0, 'EMPTY'),
    ('B002', 2.00, NULL, 1.325, 14.5, 10.5, 180.0, 'EMPTY'),
    ('B003', 2.00, NULL, 1.325, 14.5, 11.8, 180.0, 'EMPTY'),
    ('B004', 2.00, NULL, 1.325, 14.5, 13.2, 180.0, 'EMPTY'),
    ('B005', 2.00, NULL, 1.325, 14.5, 14.5, 180.0, 'EMPTY'),
    ('B006', 2.00, NULL, 1.325, 14.5, 15.8, 180.0, 'EMPTY'),
    ('B007', 2.00, NULL, 1.325, 14.5, 17.2, 180.0, 'EMPTY'),
    ('B008', 2.00, NULL, 1.325, 14.5, 18.5, 180.0, 'EMPTY'),
    ('B009', 2.00, NULL, 1.325, 14.5, 19.7, 180.0, 'EMPTY'),
    ('B010', 2.00, NULL, 1.325, 14.5, 21.2, 180.0, 'EMPTY'),
    ('B011', 2.00, NULL, 1.325, 14.5, 22.5, 180.0, 'EMPTY'),
    ('B012', 2.00, NULL, 1.325, 14.5, 23.8, 180.0, 'EMPTY');

-- 확인 — slot_code 문자열 정렬이 곧 번호순이다(A001 형식이라 자리수가 같다).
SELECT slot_code, usable_height, usable_width, fork_height,
       destination_x, destination_y, destination_heading, status
FROM storage_slot ORDER BY slot_code;

-- 기대: total 26 (랙 24 + SLOT-01/02), rack 24.
-- rack 이 48 이면 구 코드(A1~B12)가 남아 있다는 뜻이다 — 위 6번 DELETE 를 볼 것.
SELECT COUNT(*)                                AS total_slots,
       SUM(slot_code REGEXP '^[AB][0-9]{3}$')  AS rack_slots,
       SUM(status = 'EMPTY')                   AS empty_slots
FROM storage_slot;
